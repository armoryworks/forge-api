# Training Content — Adversarial Accuracy Review

Auto-generated record of the content-accuracy pass over all training modules. 49 review agents (one per seeder) re-read the real forge-ui/forge-api code and tried to *refute* each module's factual claims. **High-confidence errors were fixed in place** (EN `.cs` + ES `.json`, structure preserved); **uncertain items were flagged, not changed.**

- **Seeders reviewed:** 49  
- **Fixes applied (committed):** 107  
- **Items flagged for human review:** 123  
- **Seeders that needed fixes:** 34 (15 were already accurate)  
- Structure parity preserved (359 EN/ES 1:1), build `-warnaserror` clean, completeness PASS.

> **How to use this doc:** the **Fixes** are already live in the seeders — skim them to spot any correction you disagree with. The **Flagged** items are *open questions* the review wasn't confident enough to auto-fix — these are where your eyes add the most value.

---

## Flagged for review (open questions)

### Accounting
- **`accounting-exports-guide`** — The article (and the exports walkthrough + advanced reference) state the 'Push to QuickBooks' section appears when the install enables CAP-ACCT-QBO-EXPORT *and* a QuickBooks OAuth connection is in place. In the actual UI (exports.component.html) the section is gated SOLELY by `*appCap="'CAP-ACCT-QBO-EXPORT'"` — there is no client-side check for an OAuth connection, so the section renders on capability alone. The OAuth-connection requirement is real at the server/push level (QboExportController docs), so the framing is not flatly wrong, but the 'a second section appears' visibility claim is technically capability-only in the UI. Left unfixed — ambiguous (server vs UI level) and the framing is consistent across all three modules; a human should decide whether to tighten the wording.
- **`accounting-field-reference`** — The AP/AR Aging QuickRef and the Advanced Reference say the table 'supports up to six buckets (b0..b5)'. The SERVER (AgingBuckets.cs) does NOT cap at six — bucket count = (configurable comma-separated bounds) + 1, so 7+ buckets are possible from settings. The 'up to six' limit is purely a FRONTEND artifact: the aging/grni templates hardcode a fixed pool of b0..b5 cell templates, so a 7th bucket would have no currency cell. The claim is therefore accurate as a UI-rendering statement (which is how it's phrased), so not fixed — but flagging because the 'table supports up to six buckets' wording could mislead if read as a server/domain limit.

### Ai
- **`ai-quiz`** — Question ai5 ('The Clear Chat button (delete_sweep icon) is grayed out. Why?') is built on a false premise: in the real UI the Clear Chat button is HIDDEN when there are no messages (wrapped in @if (hasMessages())), not 'grayed out'/disabled. The correct answer ('There are no messages — nothing to clear') captures the right concept, but the scenario 'it is grayed out' cannot actually occur. The explanation also says 'disabled when there are no messages.' Rewriting a quiz question's framing is a judgment call, so flagged rather than fixed. A human may want to reword the stem/explanation from 'grayed out / disabled' to 'not shown / hidden'.
- **`ai-field-reference`** — QuickRef 'Text Input' item calls it a 'Multi-line text field' and the Overview/Walkthrough describe Shift+Enter inserting 'a new line'. The real control is a single-line <input> (ai.component.html: <input class="ai-chat__text-input">), whose onKeydown only intercepts plain Enter to send; an <input> cannot hold a newline, so Shift+Enter is effectively a no-op rather than inserting a line. The Shift+Enter = don't-send gesture is still the documented convention (and quiz ai2's answer), so this is subtle/ambiguous — flagged for human review rather than changed. Affects ai-overview, ai-walkthrough (Message Input step), ai-field-reference, and quiz ai2.
- **`ai-overview`** — Article and Field Reference say the input placeholder shows 'Ask {assistantName}'. The actual i18n key ai.askPlaceholder is 'Ask {{name}} a question...' so the real placeholder reads 'Ask <Name> a question...'. The seeder's shorthand captures the gist ('Ask <name>'), so this is a minor imprecision — flagged, not fixed.

### Announcements
- **`announcements-admin-walkthrough`** — Steps 3 and 4 both target the identical selector 'app-data-table' (one popover for the sent list, the next for opening the acknowledgment roster). The selector resolves and both popover texts are accurate, so this is not a factual error — but two consecutive steps highlighting the same element is a UX oddity a human may want to refine (e.g. point step 4 at the row eye-icon). Left unchanged since I was told not to add/remove steps.
- **`announcements-admin-overview`** — The Article/QuickRef state Target Teams 'targets which teams the announcement targets' and the response model returns targetTeamIds, but note the overlay/recipient routing for SelectedTeams vs IndividualTeam is identical in code (both filter by the user's single TeamId; there is no per-user multi-team membership). Training treats 'Selected Teams = multiple teams' vs 'Individual Team = one team' as a meaningful distinction; functionally they resolve the same way against a user's one TeamId. Not clearly wrong (you can pick multiple vs one team in the picker), so flagged rather than fixed.

### Approvals
- **`approvals-walkthrough`** — Step 6 ('Workflow List') describes each workflow row as showing 'its name, entity type, active status, and creation date' — prose description that omits the Steps-count column present in the real table. Left as-is because it reads as a non-exhaustive 'with its ...' description rather than a strict column list; flagging for consistency with the QuickRef fix in case the human wants the Steps column mentioned here too.
- **`approvals-field-reference`** — Inbox column labels in training use descriptive names that differ from the actual rendered <th> headers: training says 'Entity Type' but the header is 'Type' (approvals.cols.type), and 'Approval Step' but the header is 'Step' (approvals.cols.step). The training values describe what each column means rather than quoting the header verbatim, so this may be intentional. Not fixed (judgment call).
- **`approvals-field-reference`** — QuickRef 'New Workflow button' says it is in the 'workflows toolbar' and the walkthrough/Advanced Reference imply a toolbar. In the real UI the button lives in a div with class 'workflow-editor__header' (not a named toolbar component). 'toolbar' is a loose but plausible description; not fixed.
- **`approvals-reference`** — Advanced Reference states each decision in ApprovalRequest.decisions[] carries 'delegatedToUserName'. The field exists on ApprovalDecisionResponseModel, but GetPendingApprovals.cs always passes null for it (delegate name is never populated in the pending/list projection). The claim that the field 'ships on every request' is technically true (field present) but the value is always null in practice. Low confidence it's an error; flagging.

### Assets
- **`assets-walkthrough`** — The final 'Assets Table — Click to Open & Edit' step popover says the pencil/Edit form lets you change 'identity fields, status, notes, and tooling details'. The create/edit dialog form (assets.component.html) has NO status control — status is changed only via the detail panel's quick-action status buttons (updateStatus()), and the edit form patch (openEditAssetFromDetail) omits status. Quiz question as7 even marks 'You must open the edit dialog to change status' as the WRONG answer, confirming the inconsistency. Listing 'status' among what the Edit form changes is misleading, but the sentence could be read as describing the detail dialog broadly, so left for human review rather than fixed.
- **`assets-labels`** — The 'Scanning a Label' section lists scan-aware screens as 'Parts, Inventory, Kanban, Quality, and the shop-floor clock'. Code shows setContext is also called for 'customers' and 'leads' screens (in addition to the listed ones). The listed set matches the forge-ui CLAUDE.md 'integrated features' doc, so it is not wrong per se (just non-exhaustive) — flagged, not fixed.

### Automations
- **`automations-reference`** — Reference body says the nightly job 'nets shortfall against on-hand + in-transit + safety stock'. In AutoPurchaseOrderJob the formula is shortfall = TotalDemand - currentStock - inTransit + part.SafetyStockQty, so safety stock is ADDED to demand (it INCREASES the shortfall / order qty), not netted/subtracted alongside on-hand and in-transit. Grouping safety stock with the subtracted terms is technically misleading, though arguably defensible as 'considers safety stock'. Low impact, advanced-reference nuance — flagging rather than risking a wrong edit. Same wording mirrored in es/automations.json.
- **`automations-reference`** — Reference body says 'Source SO IDs are JSON-serialized onto the suggestion/PO notes.' For PO output the IDs are serialized into the PO notes string (correct), but for the Suggest path they are stored in the AutoPoSuggestion.SourceSalesOrderIds dedicated entity column (set via SourceSalesOrderIds = JsonSerializer.Serialize(item.SoIds)), not a 'notes' field. The 'suggestion ... notes' half is imprecise. Minor; flagging for human review. Mirrored in es file.

### Chat
- **`chat-field-reference`** — QuickRef 'Sidebar Navigation' lists 'Search (data-testid: channel-list-search) — Filters conversations and channels in real time as you type.' That search box does NOT exist in the live ChatComponent (chat.component.html — the slide-out panel + full /chat page the walkthrough targets); its only search box is the user-picker ('Search users') shown when starting a new DM. The channel-list search with data-testid=channel-list-search exists in the SEPARATE ChatChannelListComponent used by the popout (/chat/popout) and mobile chat. So the claim is accurate for the popout/mobile surface but not the slide-out panel. Ambiguous which surface the reference describes — flagging, not fixing.
- **`chat-walkthrough`** — Walkthrough step 2 popover (element .chat-panel__channel-list, the live ChatComponent) says 'Use the search box to filter' — the live panel has no conversation/channel search box (only the user-picker search appears, and only after clicking New Message). The same nuance as chat-field-reference: search exists in the popout's ChatChannelListComponent, not this slide-out/full-page panel.
- **`chat-walkthrough`** — Walkthrough step 3 and Article/QuickRef refer to 'New Group' to create a channel. In the live ChatComponent the channel-create button's tooltip/label is 'New Channel' (chat.component.html line 106, matTooltip='New Channel'); the 'New Group' label appears in the popout/mobile ChatChannelListComponent. The .action-btn--create selector the walkthrough targets actually resolves to the DM 'New Message' button. Label-attribution nuance across the two chat surfaces — flagging, not fixing.
- **`chat-reference`** — Advanced Reference lists Announcements role gating as 'List active / create / update / delete / send — [Authorize(Roles = "Admin,Manager")]'. AnnouncementsController has no distinct 'send' endpoint — creation (POST /announcements) is what dispatches. Minor wording (send == create); not clearly wrong, flagging for awareness.

### CustomerReturns
- **`customer-returns-resolving-and-closing`** — Article states that on clicking Resolve 'the inspection notes are saved'. The UI resolve dialog (customer-return-detail-panel) does collect an Inspection Notes field and POSTs { inspectionNotes } to /resolve, BUT the API endpoint POST /{id}/resolve (ResolveCustomerReturnCommand(int Id) + handler) takes no body and only sets Status=Resolved — it never reads/persists inspectionNotes. So inspection notes typed at resolve time are silently discarded by the backend. The seeder's own Advanced Reference module correctly states resolve takes 'no body'. The dialog field existing and the panel refreshing are accurate; only the 'inspection notes are saved' clause appears false. Left unfixed because it's a frontend/backend gap and rewording is a judgment call for a human.
- **`customer-returns-walkthrough`** — Step 1 lists a 'Create Rework Job' option as part of receiving a return. The create dialog UI (customer-return-dialog) does NOT expose a Create Rework Job checkbox/toggle — its form is only Customer, Original Job, Reason, Return Date, Notes. CreateReworkJob exists only on the API (CreateCustomerReturnRequestModel / POST body). The walkthrough is framed around the API call (POST /api/v1/customer-returns), so the claim is arguably correct for the API surface, but a reader could expect the option on screen. Flagging for human review rather than editing, since the module is explicitly API-oriented and the statement is true of the endpoint.
- **`customer-returns-using-the-ui`** — The 'New Return' step says the create dialog asks for 'an optional Return Date'. In the create dialog the Return Date field has Validators.required (returnDate FormControl), so it is effectively required (defaults to today). Minor; the API CreateCustomerReturnRequestModel does require ReturnDate too. Flagged, not fixed, because 'optional' may refer to the user not having to change the prefilled value and it's a low-impact wording nuance.

### Customers
- **`customers-field-reference`** — The 'Quotes Tab Table Columns', 'Orders Tab Table Columns', and 'Invoices Tab Table Columns' groups describe the Status column as 'Sortable, filterable (enum)'. In the real components (customer-quotes-tab/orders-tab/invoices-tab .ts), the status ColumnDef is sortable-only — it has NO `filterable: true` and no `type: 'enum'` (only the Estimates status column and the Interactions type column are actually filterable). Did not fix: it's a single-attribute mischaracterization and the minimal edit is a judgment call; flagging per conservative rule.
- **`customers-field-reference`** — The 'Quotes Tab Table Columns' and 'Orders Tab Table Columns' groups omit the real 'Lines' (lineCount, center-aligned) column, and the 'Jobs Tab Table Columns' group omits the real 'Job #' (jobNumber) and 'Priority' columns. The columns that ARE listed are described correctly, so this is reference-list incompleteness rather than a false statement. Cannot be fixed without ADDING QuickRef items (disallowed by the structure-identical rule), so flagging only.
- **`customers-overview`** — The Overview article and the Field Reference say clicking a row in the Quotes/Orders/Jobs/Invoices tabs opens that record's 'detail dialog'. In the actual tab components the row click navigates to a separate page route with a query param (openQuote -> /quotes?id=, openOrder -> /sales-orders?id=, openJob -> /board?job=, openInvoice -> /invoices?id=) rather than opening an in-place detail dialog. Soft characterization ('dialog' vs page navigation); flagging, not fixing.
- **`customers-walkthrough`** — The walkthrough module's Summary field (both .cs and es.json) says 'detail page with 10 tabs', and the Field Reference 'Detail Page Tabs' group lists 10 tabs (omitting Pricing). The detail page is lifecycle-driven (Active shows 11 tabs incl. Pricing, Prospect 7, Archived 6) per CustomerDetailLayoutResolverService, which the Overview article and quiz cu8 describe correctly. The '10 tabs' phrasing is a leftover simplification but lives only in summary/reference framing, not in a testable walkthrough step; flagging for consistency review rather than fixing.

### Dashboard
- **`dashboard-quiz`** — Quiz db4 asks 'The Cycle Progress bar has turned yellow. What does this indicate?' with keyed-correct answer 'You're behind schedule — completion percentage is lower than expected for the elapsed time.' The Cycle Progress widget (cycle-progress-widget.component.scss) has no yellow/behind-schedule bar state: the fill is var(--primary) and only turns var(--success) (green) at 100%; the schedule cue is the status CHIP, which turns red (--error) and reads 'Overdue' past the end date. The 'yellow bar = behind schedule' behavior does not exist, so all four options describe non-existent behavior and there is no minimal single-answer correction — the whole question premise would need rewriting (e.g. to the red/Overdue chip). Left unfixed for a human to redesign the question.

### Edi
- **`edi-field-reference`** — The 'New Trading Partner' item (Buttons and Actions group) calls it a 'Primary button in the toolbar.' The actual element is class action-btn--create (the green 'create' stereotype), and its visible label resolves from i18n key 'newPartner' = 'New Partner' (the dialog TITLE is 'New Trading Partner'). 'Primary button' is loosely wrong under the strict button taxonomy (primary = teal Save), but reads acceptably as generic emphasis. Left unfixed — judgment call. The walkthrough step 2 ('New Trading Partner') and quiz edi1 ('click New Trading Partner') are likewise conceptually fine since the button creates a trading partner; flagging for human review only if exact button-label fidelity is desired.
- **`edi-walkthrough`** — Walkthrough step 2 description says 'Click here to add a new trading partner.' The toolbar button's visible text is 'New Partner' (i18n key newPartner), not 'New Trading Partner'. The step targets the correct element (.action-btn--create) and the action is correct; only the implied label wording differs from the rendered text. Not fixed (conservative).

### Employees
- **`employees-reference`** — The Advanced Reference describes the department filter match as 'a free-text filter against EmployeeProfile.Department' and the API example as 'GET /employees?...&department=Engineering'. The actual server handler (GetEmployeeList.cs lines 81-90) performs an EXACT equality match (p.Department == dept after Trim), not a substring/Contains filter. The phrase 'free-text filter' accurately describes the input control (a free-text FormControl<string|null>) and the matched column is correct, so this is not a clear contradiction — but a reader could infer partial/substring matching that the code does not do. Conservative: flag for human review rather than fix (wording is defensible as-is). Same nuance exists in the Spanish file ('La coincidencia es un filtro de texto libre contra EmployeeProfile.Department').

### Estimates
- **`estimates-field-reference`** — Expired status is described as a 'yellow chip' (and 'Vencida (chip amarillo)' / 'Expired (yellow)'). It renders as 'chip--warning' = --warning, which is #d97706 (amber/orange) in the light theme and #fbbf24 (yellow) in the dark theme. 'Yellow' is defensible (dark theme is yellow; the convention elsewhere often calls warning 'yellow'), so I did not change it — flagging in case the team wants 'amber' for consistency. Same wording also appears in the Overview Article and the Quiz (est7).

### Expenses
- **`expenses-overview`** — Repeatedly calls the create button the "Add Expense" button; the actual visible label (expenses.createExpense) is "New Expense" (data-testid new-expense-btn). 'Add Expense' is a reasonable paraphrase of the create action, so left as-is — human may want to align to the literal label across all modules.
- **`expenses-review-decisions`** — Calls the third decision button "Request Revision" and the note field the "Review Note" field. Actual button label (expenses.requestRevision) renders "Needs Revision" and the textarea label (expenses.reviewNoteLabel) is "Notes". The i18n key is named requestRevision and the produced status is NeedsRevision, so this is label-vs-status drift; the meaning is preserved. Same wording appears in expenses-approval-queue walkthrough. Left for human review rather than rewording multiple modules + quiz options.
- **`expenses-quiz`** — ex15 (and the field-reference 'Submit button' / 'Submit button popover' items) state that HOVERING the disabled Submit button shows a validation popover. The dialog uses the <app-validation-button> stereotype: a separate warning-triangle icon to the LEFT of the button that is CLICKED to reveal the violations popover (per CLAUDE.md the legacy hover ValidationPopoverDirective is not used on new code). The popover listing violations does exist (answer core is right) but the trigger (hover vs click on a different element) is imprecise. Flagged, not fixed, to avoid over-rewording.
- **`expenses-reference`** — The 'Promotion can decline' bullet describes the legacy fallback IExpenseApPostingService posting as "(Dr Expense / Cr Cash)". Per ExpenseBillPromotionService comments, the legacy service books Dr Expense / Cr AP for AP-settled expenses and Dr Expense / Cr Cash only for cash-settled ones; the parenthetical only covers the cash subcase. Minor accounting-nuance imprecision in a Reference module — flagged for a human accounting reviewer.

### Inventory
- **`inventory-quiz`** — Quiz inv14 correct answer (option a) instructs to set 'Parent: select Rack B2 from the dropdown' in the Create Location dialog. There is no Parent dropdown in the dialog (parent is set by opening the dialog via the parent's 'Add child' button). The conceptual answer (Type=Bin, nested under Rack B2) is still correct and option (a) is still clearly the best of the four, so I did NOT edit it to avoid weakening the distractor balance — but the 'from the dropdown' phrasing is mechanically inaccurate and a human may want to reword it (e.g., 'open the dialog from Rack B2's Add child button, Name=Bin 7, Type=Bin').
- **`inventory-walkthrough`** — Walkthrough 'Search Parts' step and QuickRef 'Search / Clear' item say results update 'when you press Enter or click the search button'. The stock search app-input has no visible search button and no (keydown.enter) handler in inventory.component.html (only a Clear/close icon-button appears when there's a value); search filtering flows through searchControl.valueChanges -> searchTerm signal, and loadStock() reads it. The exact trigger wiring (does Enter call the API, or is it reactive?) is ambiguous from the template alone, so I left it. A human with runtime access should confirm how search is actually triggered.

### Invoices
- **`invoices-overview`** — Article states a Draft invoice 'You can still edit, void, or delete it' (and Sent 'can still be voided'). The invoice detail panel (invoice-detail-panel.component.html) renders only Close, Delete (Draft), Void (Draft/Sent), and Send (Draft) buttons — there is NO Edit button. The InvoiceDetailDialog declares an unused editRequested output, suggesting edit was scaffolded but never surfaced. The 'can be edited' wording may be misleading. Left unfixed: judgment call about whether edit is 'present', and fixing would touch the Article narrative + QuickRef status table + ES across multiple spots.
- **`invoices-field-reference`** — Under 'Line Item Fields (Create Dialog — Main Area)' there is a 'Total (computed)' item described as a field 'Automatically calculated as Qty x Unit Price. Not editable.' The add-line FORM row (invoice-dialog.component.html) has only 6 columns: Part ID, Part Number, Description, Qty, Unit Price, Add — no Total cell. A Total IS shown per row in the populated lines table and tfoot, so the claim is defensible as describing the table column. Borderline; not fixed.
- **`invoices-walkthrough`** — Search step (and QuickRef Search) says 'Press Enter to apply the search filter.' The list component wires BOTH (keyup.enter)=applyFilters() AND a 300ms debounced searchControl.valueChanges -> loadInvoices(), so search also applies live as you type. 'Press Enter to apply' is incomplete rather than wrong; not fixed.
- **`invoices-field-reference`** — Detail View 'Customer' item says the link 'navigates to the customer detail page.' Per CLAUDE.md/EntityLink, customer links go to the full-page /customers/:id/overview route, whereas Sales Order/Shipment links open ?detail=type:id dialogs. The wording is correct for Customer but the QuickRef does not distinguish the two behaviors; minor, not fixed.

### Kanban
- **`kanban-overview`** — The overview/walkthrough/quiz/quickref describe the irreversible-stage rule as 'cannot move BACKWARD out of an irreversible stage'. The server (MoveJobStage.cs) enforces exactly this (blocks when previousStage.IsIrreversible AND targetStage.SortOrder < previousStage.SortOrder), so the conceptual description is correct. However, the CLIENT drag handler in kanban.component.ts (onCardDropped / onSwimlaneDropped) is stricter: it returns early when the TARGET stage is irreversible (`if (!targetStage || targetStage.isIrreversible) return;`), so via drag-and-drop you cannot drop a card INTO an irreversible column at all (it snaps back), regardless of direction. Not fixed because the authoritative business rule matches the training and this is a UI-vs-server nuance, but a human may want to reconcile the wording with the actual drag behavior.
- **`kanban-field-reference`** — 'Billing Status Icon' item says 'Dollar icon. Green checkmark for Invoiced, orange pending icon for Uninvoiced.' The card actually renders Material icon 'paid' (a circled-dollar) for Invoiced and 'pending' for Uninvoiced, with CSS classes card__billing--invoiced/--uninvoiced presumably coloring them green/orange. The 'green checkmark' phrasing is slightly off (it's a paid/dollar glyph, not a checkmark) but the colors and intent are plausibly right; left as-is pending confirmation of the SCSS color mapping.
- **`kanban-field-reference`** — 'Validation Rules → Backward Move Restriction' lists the error message as 'Cannot move backward from irreversible stage.' The actual server message is longer: "Cannot move backward from irreversible stage '{stageName}'. Documents created at this stage cannot be voided." The training is a reasonable paraphrase (not a verbatim-quote claim), so not fixed.

### LeadScoring
- **`entity-completeness-overview`** — The predicate DSL section documents relationExists as {"type":"relationExists","field":"addresses","minCount":1} and all/any/not as taking a 'predicates' array. The real PredicateEvaluator.cs requires 'relation' (NOT 'field') for relationExists (EvaluateRelationExists reads only the 'relation' property) and 'of' (NOT 'predicates') for all/any/not (EvaluateAll/Any/Not read only 'of'); there is no 'field'/'predicates' fallback, so the documented intro JSON evaluates to false against the real engine. NOT fixed because the companion 'lead-scoring-reference' (Advanced Reference) module explicitly says 'Two facts override what the introductory modules imply' and documents the correct 'relation'/'of' forms — i.e. the seeder author appears to have DELIBERATELY presented a simplified-but-technically-wrong intro form and centralized the correction in the reference. Treating this as intentional pedagogy vs. an accidental error is a judgment call; flagging for a human to decide whether the intro should be corrected to the real syntax.
- **`entity-completeness-walkthrough`** — Step 'El predicado'/'The predicate' popover lists 'all/any/not composition' for the predicate engine; consistent with the intro Article it implies the simplified DSL. Same underlying concern as entity-completeness-overview: the real engine uses 'relation'/'of' keys. The walkthrough itself does not spell out the exact JSON keys, so it is only indirectly affected — flagging alongside the Article for consistency if a human decides to correct the intro syntax.
- **`lead-scoring-field-reference`** — QuickRef 'Predicate DSL — Operators' group documents relationExists as { type:'relationExists', field:'addresses', minCount:1 } and 'all / any / not' as taking a 'predicates' array. Real PredicateEvaluator requires 'relation' and 'of' respectively (no fallback). NOT fixed for the same reason as entity-completeness-overview — the Advanced Reference module deliberately corrects these intro forms, so the discrepancy looks intentional. Flagging in case the human wants the QuickRef aligned to the real API keys.
- **`lead-scoring-quiz`** — Quiz question ls9 marks option B { "type": "all", "predicates": [ {"type":"fieldPresent","field":"taxId"}, {"type":"relationExists","field":"addresses","minCount":1} ] } as the correct answer (isCorrect:true). Against the real PredicateEvaluator this exact JSON evaluates to FALSE: 'all' needs an 'of' array (not 'predicates') and 'relationExists' needs 'relation' (not 'field'). A learner copying this answer into the real editor would get an always-false predicate. NOT auto-fixed because (a) the seeder deliberately teaches the simplified form in the intro modules and corrects it only in the Advanced Reference, and (b) changing ls9's correct option to use 'of'/'relation' would make the quiz internally inconsistent with the intro Article + QuickRef it tests. This is the highest-confidence factual problem but fixing it cleanly requires a coordinated multi-module decision — flagging for human review.
- **`icp-rubrics-overview`** — The 'Saving' section states 'the save is one round-trip and produces one rolled-up activity entry'. From the API perspective the UI save is actually TWO sequential round-trips: a header PUT (/icp-rubrics/{id}) followed by a separate bulk dimensions POST (/icp-rubrics/{id}/dimensions) — the icp-rubrics.component.ts docstring explicitly says 'The save is two sequential PUTs — header first, then dimensions'. The 'one round-trip' phrasing is defensible if read as 'the dimensions bulk-save is one round-trip / one activity row' (which is true and matches the controller comment) and as 'one Save click' from the user's view, so this is borderline rather than clearly wrong. Flagging, not fixing.

### Leads
- **`leads-walkthrough`** — Step 1 popover for the New Lead button ([data-testid='new-lead-btn']) says 'Click here to create a new lead. You'll enter the company name, contact name, email, phone, source, follow-up date, and notes.' In reality openCreateLead() opens the two-step NewLeadForkDialogComponent (pick an engagement shape first, then the form), not a flat single-step form. The popover doesn't explicitly claim a flat/single-step form and every field it lists does appear in the fork dialog, so it is not strictly wrong — but it under-describes the engagement-shape fork step. Left unfixed (judgment call; the dedicated 'leads-new-lead-fork' article covers the fork accurately). A human may want to add a note that this opens the engagement-shape picker.
- **`leads-overview`** — The Convert-to-Customer section correctly describes a one-click action with no multi-step dialog (matches convertLead() in lead-detail-panel.component.ts which POSTs {createJob:false} and navigates). However, the backend ConvertLead handler DOES support createJob and credit/tax/address inputs, and a stale code comment in lead-detail-panel.component.html (lines 211-215) still describes 'opens the stepper ... toggles Also create a job'. The user-facing behavior matches the training (one-click), so no fix — but flagging that the backend retains the richer convert capability in case product direction reintroduces the stepper UI.

### Maintenance
- **`maintenance-reference`** — Data Dictionary claims `WasAccurate` is a 'boolean outcome flag set when the prediction is resolved or feedback is recorded.' The 'set when the prediction is resolved' half is false: ResolvePredictionHandler (forge-api/.../Features/Maintenance/ResolvePrediction.cs) sets only Status + ResolutionNotes and never touches WasAccurate. WasAccurate is actually set ONLY by RecordPredictionFeedbackHandler (= ActualFailureOccurred) and by MarkFalsePositiveHandler (= false). Flagging rather than fixing because the right rewording (resolve does NOT set it; false-positive sets it false; feedback sets it from the actual outcome) is a judgment call in a power-user reference and the surrounding narrative ('feedback drives accuracy') is otherwise sound.
- **`maintenance-reference`** — The Performance section says 'Performance metrics are fed by the resolve / false-positive / feedback loop.' Strictly, Resolve does not update WasAccurate (the accuracy signal), so including 'resolve' as a feeder of accuracy metrics is questionable — only false-positive and the feedback endpoint move WasAccurate. Borderline/loose phrasing; flagged for the same reason as the WasAccurate item.
- **`maintenance-overview`** — Article (and QuickRef/Walkthrough) describe 'Schedule Maintenance' as simply 'Available on Predicted or Acknowledged items' with no role caveat, but the controller gates POST /{id}/schedule-maintenance with [Authorize(Roles = "Admin,Manager")] (PredictiveMaintenanceController.cs line 43). Acknowledge/Resolve/FalsePositive have NO role restriction. The main modules don't assert role-independence (no hard contradiction), so this is an omission, not an error — but a non-Admin/Manager user will see the Schedule action fail with 403. The Advanced Reference correctly documents this gating. Flagged in case the team wants the main modules to mention the role requirement.
- **`maintenance-field-reference`** — QuickRef labels several KPI cards as 'Active Predictions', 'Pending Acknowledgment', 'Maintenance Scheduled' and the Schedule action as 'Schedule Maintenance'. The actual UI i18n labels are terser: 'Active', 'Pending ack', 'PM scheduled', and the Schedule button tooltip/aria-label is 'Schedule PM'. These are descriptive paraphrases rather than literal label text, so not fixed — flagged only if exact-label fidelity is desired.

### Mfa
- **`mfa-walkthrough`** — Step 2 element selector '.account-page__actions .action-btn--primary' is ambiguous. The security page has three '.account-page__actions .action-btn--primary' buttons in DOM order: Change Password, Set PIN, then Enable Two-Factor Authentication. driver.js resolves the selector via document.querySelector (first match), so the popover titled 'Enable / Add Device Button' would actually highlight the Change Password button, not the Enable MFA button. Not fixed because correcting it requires changing the walkthrough 'element' selector, which the task's structure-preservation rule forbids. A human should decide whether to add an MFA-specific selector/test-id to the Enable button and update the step.
- **`mfa-overview`** — Beyond the clause already fixed, the 'Registering More Than One Device' section still frames a second device as a login fallback: 'Having two devices means a lost or wiped phone no longer locks you out: you simply authenticate from the other one' and the practical-setup line 'a dead battery on the floor doesn't strand you at the login screen'. Because the login challenge binds exclusively to the default device, a non-default backup authenticator cannot be used at the challenge screen if the default device is lost — recovery codes are the real fallback. Left unfixed because correcting it well requires editorial rewriting of the surrounding narrative (judgment call) rather than a minimal swap.
- **`mfa-reference`** — The 'Device Lockout & Recovery' section lists '**A second registered device** — if you added a backup authenticator (the Add Device flow), authenticate from it instead' as a recovery path when you lose your authenticator. Same root issue as the overview: the login challenge is bound to the default device only (CreateChallengeAsync/ValidateChallengeAsync), so a non-default backup device cannot satisfy the challenge unless it has become the default. Flagged rather than fixed because the precise correct wording is an editorial judgment call.
- **`mfa-quiz`** — Question mfa9's correct option (b: 'used first during login challenges') is correct and the distractors are clearly wrong, so the quiz still works. However the trailing explanation says 'You can still use any registered device, but the default is the primary one presented', which (like the overview) overstates non-default device usability at login. Did not edit since the answer key is correct and changing only explanation prose is a judgment call.

### Navigation
- **`navigating-the-app-overview`** — The 'Sidebar sections' flat bullet list materially misrepresents the real navigation: the sidebar is a hierarchical drill-down with groups (Operations, Sales, Production, Inventory, Purchasing, People, Insights, plus dark Accounting; Dashboard pinned top; Shop Floor + Admin pinned bottom). The list shows flat top-level items and includes 'Chat' (a header icon, not a sidebar item) and 'Account' (reached via the avatar menu, not the sidebar); Calendar/AI/Training are actually nested under groups. Left unfixed because a faithful rewrite is a large structural change with high risk of introducing new errors — recommend a human rewrite the section to the group model in nav-tree.service.ts.
- **`navigation-field-reference`** — The 'Sidebar width' item says state is 'saved to user preferences'. Actual persistence is localStorage key 'forge-sidebar-collapsed' (per-browser, not a server-synced user preference). Minor wording; left as-is since 'user preferences' is arguably loose-but-not-false. Flag for consistency with the corrected Overview/Persistence wording.
- **`navigation-field-reference`** — The 'Search icon (magnifying glass) — Opens global search overlay' framing implies an icon you click to open an overlay. In the desktop header the global search is an always-visible inline input (decorated with a magnifying-glass icon and a Ctrl+K hint), not a click-to-open overlay; it is hidden on tablet/mobile. Only the shortcut was corrected (to Ctrl+K); the overlay framing was left for human judgment.

### Notifications
- **`notifications-reference`** — The Reference body refers to the server entity as `AppNotification.IsDismissed`. The actual C# entity class is `Notification` (forge.core/Entities/Notification.cs, class Notification); the only `AppNotification` is the Angular frontend model (app-notification.model.ts). Strictly, the server-side references should be `Notification`, not `AppNotification`. Left unfixed because the umbrella CLAUDE.md entity list itself names the entity `AppNotification`, making the intended naming genuinely ambiguous (a human should confirm whether the entity was renamed/aliased).
- **`notifications-field-reference`** — The 'Source' field item describes source as only 'user' or 'system'. That is correct for the panel's Messages/Alerts tab logic (source==='user' / source==='system'), but the standalone Notifications page Source filter and the QuickRef/Walkthrough also list 'board' and 'timer' as valid source values. The binary user/system description is a simplification, not a contradiction, so left as-is for human judgment on whether to broaden it.

### Onboarding
- **`setting-up-your-profile`** — The Profile Section instructs the user to go to Account → Profile and "confirm your work location" (and the Why-It-Matters paragraph says an incomplete work location stops the system from picking a state withholding form). The actual Account → Profile page (account-profile.component.html) has only First/Last Name, Initials, Date of Birth, Gender, and Avatar Color — there is NO work-location field, and no 'workLocation' reference exists anywhere in the account feature. In the real app, state withholding is driven by the home-address STATE entered in the onboarding wizard Step 2, not a profile work-location selection. (WorkLocationId does exist on the user entity per CLAUDE.md, possibly admin-set, so I'm not 100% sure it's nowhere employee-facing.) Not fixed because correcting it would require non-minimal rewriting of two paragraphs and the conceptual model.
- **`onboarding-quiz`** — Question ob6 ('You want to see how complete your employee profile is. Where do you look?') marks 'Dashboard → Profile Completeness widget' as the correct answer, and the explanation describes a Dashboard widget that lists missing items. The dashboard widget registry (widget-registry.ts) has no Profile Completeness widget (widgets are Today's Tasks, Jobs by Stage, Team Load, Deadlines, Activity, Margin Summary, Cycle Progress, Open Orders, End of Day, Action Items, Inventory Snapshot). Profile completeness is actually surfaced as per-item complete/incomplete (yellow warning) indicators in the Account sidebar (account-sidebar.component), not on the Dashboard. The marked-correct option points to a nonexistent widget, but none of the other three options is correct either, so fixing it would require non-minimal rewording of the correct option and its explanation — flagged rather than fixed.

### PageSpecific
- **`currencies-overview`** — Article says 'New Exchange Rate (enabled once at least two currencies exist)'. In currencies.component.ts the button is always enabled; clicking it with <2 currencies shows a 'needTwoCurrencies' error snackbar rather than the button being disabled/gated. Wording is close to true (you can only successfully use it once 2 exist) so left as a flag, not a fix.

### Parts
- **`parts-overview`** — States 'eleven viable (procurement × inventory class) combinations are documented.' The current code disagrees with itself: new-part-fork-dialog.component.ts has a viableCombos array with 14 entries (Make×4, Buy×6, Subcontract×2, Phantom×2) and register-part-workflow-steps.ts references 'the 14-combo seed', yet a stale doc-comment in the same fork dialog still says '11 viable combos'. The parts-reference module already describes the 14-combo set (M1–M4/B1–B6/S1–S2/P1/P3). 'Eleven' is likely stale (should be 14) but the codebase is internally inconsistent — flagging rather than guessing.
- **`parts-overview`** — The 'Detail Page' tab list includes 'UoM — stock / purchase / sales units of measure' and 'Activity — chronological log' as tabs. Verified: there is no 'uom' tab in PartDetailTabId nor a uom @case in the panel @switch (PartUomClusterComponent is imported but never rendered as a tab; purchase UoM surfaces under Sources via part-purchase-units-cluster). Activity is a persistent footer, not a tab. Both are likely wrong-as-tabs, but these are loose 'common tabs' enumerations that partly describe real clusters/fields, so I left them for human review rather than restructure the list.
- **`parts-field-reference`** — The 'Common tabs' item lists 'Identity, Inventory, MRP, Cost, Pricing, Material, UoM, Quality, Routing, BOM, Sources, Alternates, Purchase History, 3D Viewer, Activity, Files.' UoM and Activity are not rendered tabs (see overview flag). 3D Viewer and Serials are real conditional tabs added by the panel outside the resolver. Removing UoM/Activity would be defensible but is an invasive list rewrite on an explicitly-illustrative enumeration — flagging for human review.
- **`parts-quiz`** — pt9's explanation gives an example Make+FinishedGood tab sequence ending '...Routing, BOM, Sources, Alternates, Purchase History, Activity, Files' (lists Activity as a tab) and includes 'UoM' in the layout prose. Same UoM/Activity-as-tab inaccuracy as the field-reference 'Common tabs' flag. Left the supplementary explanation prose unchanged; only the graded option b assertion was fixed.
- **`parts-field-reference`** — Barcode Scanner Integration → 'Supported scanners' claims 'USB barcode scanners (keyboard wedge mode) and NFC readers (ACR122U with NTAG215 tags).' scanner.service.ts contains no reference to ACR122U / NTAG215 or any specific NFC hardware (it's a generic keyboard-wedge keystroke detector). The keyboard-wedge part is consistent with the code, but the specific hardware models are unverifiable from the UI/API and may come from a hardware/procurement doc — flagging rather than refuting.

### Payments
- **`payments-walkthrough`** — Step 1 popover says the Record Payment button 'is only available in standalone mode (no accounting provider connected)'. In payments.component.html the new-payment-btn is rendered UNCONDITIONALLY inside <app-page-header>; only the accounting-managed banner is gated by @if (!isStandalone()). The button is therefore visible even in integrated mode. This may be a UI bug (the accounting-boundary spec says the feature should be hidden/read-only) rather than wrong training, and the same claim recurs in the Overview, QuickRef, and Quiz pmt4, so I left it for human review rather than fixing across modules.
- **`payments-field-reference`** — QuickRef 'Buttons and Actions' → 'Record Payment (primary button, page header)' says 'Only visible in standalone mode', and 'Accounting Boundary Behavior' → 'Integrated mode' says 'Record Payment button hidden'. Same finding as the walkthrough: the button is not actually gated by isStandalone() in the current template. Flagged (not fixed) because it is plausibly an app-side gating bug and is asserted consistently across several modules.
- **`payments-quiz`** — pmt4 correct answer (option b) bundles three facts: 'blue banner appears' (correct), 'Record Payment button is hidden' (NOT matched by current code — button is unconditionally rendered), and 'data becomes read-only' (correct intent). Because the button-hidden sub-claim is the only inaccurate part and it is fused into an otherwise-correct single option, I did not edit it; flagged for human decision alongside the broader gating question.
- **`payments-reference`** — The 'Related boundary context' section states that in integrated mode 'the create dialog — and therefore these two inputs — is hidden'. The create dialog is opened by openCreateDialog(), wired to the always-visible new-payment-btn with no isStandalone()/capability guard, so the dialog is not actually hidden in integrated mode in the current code. Same root cause as the button-gating concern above; flagged rather than fixed.

### Payroll
- **`payroll-reference`** — Multiple modules (payroll-reference body, payroll-field-reference 'Admin: Sync Payroll' item, payroll-overview body) state that an Admin/Manager/OfficeManager can trigger a manual payroll sync 'from the panel' / via a 'Sync Payroll' button. The API endpoint POST /api/v1/payroll/sync exists (PayrollController.SyncPayrollData) and PayrollService.syncPayroll() exists in the UI service, BUT no component wires a Sync button — the admin user-compliance-panel (Admin -> Compliance) has only a 'Send Reminder' button and pay-stub/tax-doc upload forms; grep finds no syncPayroll()/payroll-sync usage in any template. So the capability exists at the API layer but there is no actual button in the panel. Did not fix because the endpoint/service genuinely exist and it's unclear whether the button was removed, never surfaced, or is intended elsewhere — needs an owner call on whether to soften the wording or add the button.
- **`payroll-overview`** — The Overview body italicizes the admin section as '*Per-Employee Compliance*' and payroll-field-reference ('Where Admins Upload' -> Location) and payroll-reference body refer to the 'Per-Employee Compliance' section/panel. The actual UI heading (admin.component.html line 561, i18n key admin.perUserCompliance) is 'Per-User Compliance'. Likely a literal label mismatch, but did not fix because it may be intended as a descriptive paraphrase (per-employee == per-user) rather than a literal UI-label quote, and it is not misleading.
- **`payroll-field-reference`** — Deduction categories are presented as literal enum values (e.g. 'Enum. One of 15 categories: ...HSA, FSA...', plus standalone category items labeled 'HSA' and 'FSA', the pr4 quiz explanation, and the payroll-reference body backtick list). The actual C# enum members are 'Hsa' and 'Fsa' (PayStubDeductionCategory.cs), and with the default JsonStringEnumConverter (no naming policy, Program.cs) they serialize literally as 'Hsa'/'Fsa'. Every other value in those lists is the exact PascalCase member name, so HSA/FSA is inconsistent/incorrect as a literal enum identifier. Did not fix because it is a judgment call between the common acronym (correct human term, used correctly in the Overview prose) and the literal member name, and touching it would require surgical edits across many spots in both files (with the standalone item labels HSA/FSA) where a slip risks breaking JSON.
- **`payroll-quiz`** — Quiz pr8 correct option (c) text 'No — only Admins and Office Managers can delete payroll records' and its explanation omit the Manager role. The DELETE endpoints (PayrollController) carry [Authorize(Roles="Admin,Manager,OfficeManager")], so Managers can also delete; the rest of the seeder (field-reference Access Control, reference body) correctly says Admin/Manager/Office Manager. Did not fix because option (c) is still unambiguously THE correct answer to the asked question (the only 'No'; all distractors wrongly let employees delete), so the omission does not make the answer wrong — it is an accuracy improvement, not a refutation, and editing quiz option text is higher-risk than flagging.

### Planning
- **`planning-overview`** — Overview 'Daily Top 3' section claims 'The Top 3 feeds into the cycle board so everyone can see what's being worked on.' A Top-3/EOD prompt widget does exist (dashboard eod-prompt-widget.component) but it saves a free-form textarea to per-user UserPreferencesService keyed by date (eod:YYYY-MM-DD); it is NOT wired to the cycle board and is not visible to 'everyone'. The 'feeds into the cycle board' claim contradicts the code. Left unfixed because the broader Top-3 concept is a documented design feature and a clean minimal rewrite isn't obvious without inventing replacement text — human should decide whether to soften/remove the 'feeds into the cycle board' sentence (mirrored in es planning.json).
- **`planning-field-reference`** — QuickRef 'Cycle Entry Fields > Assignee' says the assignee is 'Shown as name text in the entry row.' The cycle-board template does render entry.assigneeName conditionally, BUT both server handlers (GetCurrentPlanningCycle.cs and GetPlanningCycleById.cs) hardcode the AssigneeName positional arg to null, so it never actually shows on a cycle entry in practice. (The backlog-panel avatar with initials, also mentioned in the same item, IS correct.) This is a server-side defect rather than a clear doc error describing intent, so flagged not fixed. Same applies to any Overview/walkthrough mention of an entry-row assignee.
- **`planning-overview`** — Overview/QuickRef/Reference repeatedly assert that on 'Complete (No Rollover)' the incomplete entries 'return to the backlog'. In code (CompletePlanningCycle.cs) completing without rollover only sets cycle.Status=Completed and does nothing to the entries — there is no logic that moves jobs back to a backlog; they simply are no longer committed to an active/planning cycle, so the planning page's backlog (which excludes only the selected cycle's committed jobs) would show them again. The phrasing is arguably accurate from the UI's perspective but there is no explicit 'return to backlog' operation server-side. Borderline; flagged for human judgment, not fixed.

### ProductionLots
- **`production-lots-field-reference`** — Authorization group says 'All three endpoints share the same role requirement.' The LotsController actually exposes FIVE endpoints (GET list, POST, PUT, DELETE, GET trace) — all carrying the same [Authorize(Roles="Admin,Manager,Engineer,ProductionWorker")]. The 'three' is internally consistent with the 3 endpoints documented in this same module's 'API Endpoints' group (PUT/DELETE are intentionally omitted here and covered in the Advanced Reference module), so the statement is not strictly false, just potentially misleading. Left unfixed because it is accurate for the endpoints this module documents and changing it would touch the structural framing. Human may want to soften to 'these endpoints' or note the API-only PUT/DELETE.
- **`production-lots-create-trace-walkthrough`** — Walkthrough steps 2 ('Search Lots') and 4 ('Quantity Is Decimal') both anchor their popover to the .page-header element. Step 4's content describes the Quantity field, which lives in the New Lot create dialog (not in .page-header) and is not visible during the on-screen tour since the dialog isn't open. This is the same deliberate 'anchor a conceptual note to a visible element' pattern used elsewhere in these tours (e.g. step 6 anchored to app-data-table while describing the detail panel), and the popover text itself is factually correct (step='0.0001' confirmed in lot-dialog.component.html). Flagged as a UX/judgment call, not a factual error — no fix applied.

### PurchaseOrders
- **`purchase-orders-walkthrough`** — The 'Ordered vs. Received' step anchors on 'app-data-table thead th:nth-child(7)', which is the Received column header (Ordered is nth-child(6)). The popover title is 'Ordered vs. Received' and its description covers BOTH the Ordered and Received columns, so it currently anchors on one of the two columns it describes. This is a judgment call (arguably should anchor on nth-child(6)=Ordered, which the description leads with) rather than a clear error — left unfixed for human review.
- **`purchase-orders-shipping-currency-freight`** — The article lists three Freight Allocation Methods (By Extended Value, By Quantity, Manual). The FreightAllocationMethod enum actually has FOUR values, including ByWeight. NOT an error: the actual receive-dialog UI (allocationOptions) only surfaces the three the training names (ByExtendedValue, ByQuantity, Manual) and omits ByWeight, so the training correctly describes the UI. Noting only in case product intent is to expose ByWeight later.

### Purchasing
- **`purchasing-field-reference`** — QuickRef 'Vendor Response Statuses' lists 'Awarded (green chip)'. The detail-dialog status-column mapping (getResponseStatusClass) maps responseStatus 'Awarded' -> 'chip--primary' (teal, var(--primary)), NOT green. However a SEPARATE green 'Awarded' badge (chip--success) is rendered in the vendor cell when row.isAwarded is true (rfq-detail-dialog.component.html line 166). So a green Awarded chip genuinely appears in the responses table even though the status chip itself is teal. Ambiguous/defensible, so not fixed — human may want to clarify which chip is meant. (Same green-vs-teal nuance underlies the Overview Article line: 'The winning response is marked Awarded (green)'.)
- **`purchasing-field-reference`** — QuickRef 'Detail View Read-Only Fields' -> 'Status' says it is 'shown as text in the detail header area'. In rfq-detail-dialog.component.html the dialog header/title is the RFQ number (line 1), and Status is rendered as plain text inside the info-grid body (lines 19-22), not in the header. 'Header area' is imprecise but vague enough to be a judgment call, so not fixed.
- **`purchasing-overview`** — Overview Article and Walkthrough 'Search RFQs' step both say search covers 'RFQ number, part number, and part description.' The GetRfqs handler (GetRfqs.cs) also matches the RFQ's own Description field (r.Description.Contains(term)) in addition to those three. This is an omission/incompleteness rather than a contradiction, so not fixed — could be expanded to mention RFQ description.

### Quality
- **`quality-reference`** — After the code-name fix, the Advanced Reference still frames `CAP-QC-COMPLIANCE-FORMS` as a 'QC capability for features not yet surfaced on the Quality page.' In reality that capability is the HR-side W-4/I-9/state-withholding compliance-forms pipeline (auto-extract PDF form definitions), which is surfaced in HR/onboarding, not a latent Quality-page feature. The grouping is arguably misleading even with the corrected code. Left the framing for a human to decide.
- **`quality-reference`** — The Advanced Reference says `CalibratedById` is 'stamped from the auth context, not the request body.' The handler (CreateCalibrationRecord.cs) sets `CalibratedById = 0` with a comment '// Will be set by middleware/auth context'. The intended behavior matches the claim, but the handler itself writes 0 and relies on a middleware step I did not locate/verify. Low-confidence; not changed.
- **`quality-overview`** — Overview SPC section says 'detects OOC conditions automatically' on data entry and the quiz/walkthrough describe Western Electric rule detection. The OOC EvaluateControlRules logic exists in SpcService, but I did not confirm it is actually invoked on the RecordSpcMeasurements path (the data-entry flow). The detection wiring is plausible but unverified end-to-end; left as-is.

### Quotes
- **`quotes-field-reference`** — The 'Line Item Fields (Create Dialog — Main Area)' Part item says the autocomplete 'Displays as "PartNumber — Description"'. The create dialog actually maps each option to `${p.partNumber} — ${p.name}` (partNumber — the part's short Name/identifier), not the long-form description. Borderline labeling nuance (Name vs Description after the Phase-4 Name/Description split) — left for human review rather than fixing. The walkthrough and other modules do not repeat this exact phrasing.

### Reports
- **`reports-reference`** — Cheat sheet annotates AR Aging, Revenue, and Profit & Loss as '(standalone-mode only)'. The accounting boundary is a real product concept (CLAUDE.md lists AR Aging / Revenue / P&L as standalone-mode features), but the Reports sidebar (reports.component.ts) renders these report nav-items unconditionally with no *appCap/standalone guard, and ReportsController has no standalone gate on ar-aging/revenue/simple-pnl. Whether the data degrades in integrated mode wasn't visible in the code I read, so the '(standalone-mode only)' note may be misleading about the report's visibility. Left for human review — not fixed.
- **`reports-overview`** — Overview says CSV export is 'Available in the Report Builder' and PDF is 'Server-side PDF generation via QuestPDF'. The in-builder CSV button is real (client-side Blob in report-builder.component.ts). PDF/Xlsx exist only via the server saved-report export endpoint (GET /{id}/export), not as an in-builder button — the Overview wording is arguably ambiguous but not clearly wrong, so left unchanged.
- **`reports-field-reference`** — Parts entity description text reads 'with part number, type, status, material, vendor, tooling' but the actual Parts entity exposes ProcurementSource/InventoryClass/Status (no 'type' or 'material' field). This is a descriptive-prose mismatch (not a count); judgment call on whether 'type/material' is loose shorthand. Only the field count was corrected; the prose left for human review.

### SalesOrders
- **`sales-orders-reference`** — Schedule Tab claims that on an at-risk line, past (non-deliveryDate) milestones "render as Overdue rather than merely Done." The actual code (schedule-timeline.component.ts buildMilestoneItems, line ~62) assigns status='completed' for past non-deliveryDate milestones even when m.isAtRisk is true — only the deliveryDate milestone renders overdue when past. So the rendered behavior contradicts the training. NOT fixed because the component is self-contradictory: its own comment says "mark past items as overdue" but the code assigns 'completed', so it's ambiguous whether the code or the training is the bug. Human should reconcile (likely a UI bug).
- **`sales-orders-field-reference`** — The "Line Item Table Columns (Detail View)" group lists Shipped and Remaining as two separate columns. The detail-panel Lines tab actually renders a single combined "Shipped/Remaining" column (one <th>, shipped value colored green/yellow then "/ remaining"). The data described is accurate; only the column granularity differs. Judgment call — left as-is.

### Scheduling
- **`scheduling-field-reference`** — QuickRef 'KPI Chips' > 'Scheduled Ops' value says 'Total count of operations currently on the schedule.' The component computes this chip as ganttOps().filter(o => o.status === 'Scheduled').length (scheduling.component.ts lines 109-111), and the gantt query (GetGanttData.cs) returns ALL non-Cancelled ops (Scheduled + InProgress + Complete). So the chip is the count of operations whose status is exactly 'Scheduled', NOT the total of all operations on the schedule. The word 'Total' is misleading. Borderline (a reader could interpret 'on the schedule' as 'in Scheduled state'); flagging rather than fixing per conservatism. The same subtlety is in the walkthrough step-1 'Scheduling KPIs' description ('count of Scheduled Operations').
- **`scheduling-field-reference`** — QuickRef 'KPI Chips' > 'Work Centers' value says 'Count of work centers (primary-color value).' The component computes totalWorkCenters as workCenters().filter(wc => wc.isActive).length (scheduling.component.ts lines 115-117) — i.e. it counts only ACTIVE work centers, not all work centers. Subtle omission ('count of work centers' isn't strictly false in casual terms); flagging for human review rather than fixing.
- **`scheduling-walkthrough`** — Walkthrough step 5 ('Lock an Operation') anchors to selector '.tab-panel app-toolbar', but the lock icon described in its popover lives in a data-table row cell (rendered via the appColumnCell='isLocked' template in scheduling.component.html lines 42-46), NOT in the toolbar — the gantt-tab toolbar only contains the Run Scheduler button. The popover text ('each operation row carries a lock icon') therefore describes an element other than the one it points at. Could not confirm whether this anchor mismatch is intentional (anchoring to a stable always-present element vs a data-dependent row); flagging rather than re-pointing the selector, since a row may not exist when no operations are scheduled.

### Shipments
- **`shipments-walkthrough`** — Walkthrough step 2 ("Search Shipments") claims: "Search by shipment number, Sales Order number, customer name, carrier, or tracking number. Press Enter to apply the search. Results update in the table below." In the real code this is NOT true: shipments.component.ts wires searchControl to (keyup.enter)=applyFilters() -> loadShipments(), which calls shipmentService.getShipments(undefined, status) and never passes the search term to the API. The service's getShipments(salesOrderId?, status?) has no search param, and the data-table receives [data]=shipments() directly with no page-level text filter applied. So the search box is effectively a no-op for filtering the list. The element itself exists and its intent is clearly search, so I did not edit it (the minimally-correct replacement wording is a judgment call about intended vs. actual behavior, and a wrong 'fix' is worse than a flag). Recommend a human decide whether to fix the UI wiring or soften the training text.

### ShopFloor
- **`shop-floor-clock-kiosk`** — The article states the manual-login (Clock In Manually) failure message is 'Invalid credentials' (ES: 'Credenciales no válidas'). The actual i18n string shopFloor.invalidCredentials is "Invalid email or password. Please try again." This is a loose paraphrase of the real message rather than an exact quote. Low confidence it rises to a factual error worth fixing; flagging for human judgment.
- **`shop-floor-kiosk-setup`** — The Setup article quotes the admin sign-in failure as an inline 'Invalid admin credentials' error (ES: 'Credenciales de administrador no válidas'). The actual i18n string shopFloor.invalidAdminCredentials is "Invalid credentials. Admin access required." Again a paraphrase, not an exact match. Also note kiosk-setup.component.ts performs a plain authService.login() with no client-side admin-role gate (gating is presumably server-side on loadTeams/setupTerminal); the article's framing of admin sign-in as a 'deliberate gate' is reasonable but the UI itself does not enforce a role check. Flagging for human review.
- **`shop-floor-overview`** — Overview article says the live clock displays time in 'HH:MM:SS' format, while the Field Reference says 'HH:MM:SS AM/PM'. The actual code uses toLocaleTimeString('en-US', {hour:'2-digit',minute:'2-digit',second:'2-digit'}) which renders 12-hour with AM/PM, so the Field Reference is the more precise one and the Overview's 'HH:MM:SS' is a simplification. Minor inconsistency, not clearly wrong; flagging rather than editing.

### TimeTracking
- **`time-tracking-walkthrough`** — Step 1 anchors the popover to the whole 'app-page-header' element and titles it 'Start Timer', but the header also contains the Manual Entry button, and when a timer is already running the button reads 'Stop Timer' (the @else branch). The popover is a reasonable header-level anchor and the start-timer-btn data-testid lives inside it, so this may be intentional — flagging as a judgment call, not fixing.
- **`time-tracking-overview`** — Type badge / Timer entry color is consistently called 'blue' across Overview, Walkthrough, Field Reference, and Quiz, but the SCSS uses var(--primary) (the app's teal primary) for the Timer/--timer badge, not literally blue. This is a consistent color-naming imprecision throughout the module rather than a code contradiction, so I did not change it.

### admin
- **`admin-quiz`** — Q ad7's correct answer instructs filtering the Audit Log by 'entity type SystemSetting'. The audit-log-panel entityTypeOptions dropdown (audit-log-panel.component.ts ~L52-73) does NOT include SystemSetting as a selectable option (options stop at Job/Part/Customer/Vendor/Expense/Invoice/Payment/User/PO/SO/Quote/Shipment/TimeEntry/Asset/ApplicationUser/BiApiKey). However SystemSetting IS a BaseAuditableEntity so changes are recorded, and GetAuditLog.cs filters by an arbitrary EntityType string, so the data is queryable; the answer is also still the clear best-of-four vs the distractors. Left unfixed — the conceptual answer is correct and editing option text risks the one-correct structure. Human may want to soften the 'filter by entity type SystemSetting' wording.
- **`audit-log-monitoring`** — The 'Common Use Cases' group gives concrete how-to steps: 'What changed in a setting? -> Filter by entity=SystemSetting' and 'Who approved a compliance form? -> Filter by entity=ComplianceFormSubmission, action=Update'. Both entity-type values exist at the data/API level (both are BaseAuditableEntity; GetAuditLog filters by arbitrary EntityType string) but neither is exposed as a selectable option in the audit-log filter dropdown, so a user cannot pick them from the UI as written. Ambiguous (true at API level, not surfaced in UI) — flagged rather than fixed.
- **`admin-field-reference`** — Track Type 'Code (required)' is described as 'Immutable identifier' and 'Stage Code' as 'Immutable identifier per stage', but the track-type-dialog (track-type-dialog.component.html/.ts) renders both the code FormControl and per-stage code inputs as freely editable when editing an existing track type (no readonly/disabled). Immutability is not enforced in the UI. Soft/semantic claim — flagged, not fixed.

### backlog
- **`backlog-walkthrough`** — The 'Jobs Table' (app-data-table) step lists the filterable columns as 'Stage, Priority, Assignee, Customer, Due Date' but omits Job Number and Title, which are ALSO filterable (filterable:true, type text) per backlog.component.ts. Not a contradiction (the listed columns ARE filterable; the list is just illustrative/incomplete), so left unchanged — flagging in case the author wants the full filterable set named.
- **`backlog-field-reference`** — Cancel button item says 'No confirmation unless the form has unsaved changes (dirty form guard)'. The job dialog passes [draftFormGroup]/[draftConfig] and DialogComponent has a dirty-confirmation path, so this is plausibly accurate, but I did not fully trace DialogComponent's close/backdrop confirmation behavior to confirm the exact trigger. Low risk; flagged for completeness, not fixed.

### calendar
- **`calendar-field-reference`** — Month View group 'Day Cell — PO Events' states PO events show 'a local_shipping icon, PO number, and vendor name.' In Month view (calendar.component.html lines 78-87) the visible PO chip renders only the icon + po.poNumber; vendor name appears only in the title tooltip / aria-label, not as visible text. Vendor name IS visibly shown in Week view, so the claim is partially true across views — flagging rather than fixing since 'vendor name' is present (tooltip) and the statement is not clearly false in spirit.
- **`calendar-field-reference`** — Day View group 'PO Deliveries Section' lists displayed elements as 'PO number, vendor name, expected delivery date, status, and line count.' The Day-view PO card (lines 212-222) renders poNumber, vendorName, a status chip, and lineCount — it does NOT render a discrete 'expected delivery date' field (the date is implicit in the day being viewed). Conceptually the expected-delivery-date is why the PO appears on that day, so flagging not fixing.
- **`calendar-field-reference`** — Day View 'Job Cards' meta-row item (and quiz cal9 correct answer) says it shows 'assignee name'; the template binds {{ job.assigneeInitials }} (initials, not full name). The same QuickRef's 'Job Chip Elements > Assignee' item correctly notes 'initials and color in some views.' Imprecision (initials vs name); quiz answer remains correct vs its distractors, so not fixed.

### compliance
- **`compliance-forms-field-reference`** — QuickRef 'Form Actions and Buttons' > 'Onboarding Wizard Banner' calls it a 'Yellow banner'. The actual element .tax-forms__wizard-banner (account-tax-forms.component.scss) uses background color-mix of var(--primary) and a border of solid var(--primary). --primary is teal (#115e59 family), NOT yellow. Color description appears wrong, but color naming is subjective and I could not render the live UI — flagging rather than editing.
- **`compliance-forms-field-reference`** — QuickRef 'Required Badge' says 'Red Required text'. The element .tax-forms__item-required uses color: var(--warning) (#d97706 amber/orange in light, #fbbf24 in dark) — there is a separate --error red token (#dc2626) it does NOT use. 'Red' is likely inaccurate (should be amber/orange/warning). Same 'red Required badge' wording also appears in the walkthrough step 1 and quiz cf7 explanation. Color call is borderline/subjective so flagged for human review rather than fixed across all three places + Spanish.
- **`compliance-forms-field-reference`** — QuickRef 'Onboarding Badge' says 'Blue Completed in onboarding wizard badge'. The element .tax-forms__item-badge uses color: var(--primary) (teal), italic. Text string 'Completed in onboarding wizard' is exactly correct in the HTML, but the color 'Blue' is questionable (it's teal/primary). Subjective color claim — flagged, not fixed.
- **`compliance-forms-walkthrough`** — Step 2 anchors the popover to .account-page__title (the page <h1> header, which has a STATIC 'description' icon) but the description says 'The icon changes based on status... or a green check_circle when completed.' The icon that actually changes per form-status is .tax-forms__item-icon on each form card, not the page title. The popover text describes real app behavior but is attached to the wrong element. Walkthrough-authoring/anchor nuance — flagged rather than re-pointing the selector (which could break the tour) per conservative guidance.

### events
- **`events-field-reference`** — QuickRef 'Save / Create Event (dialog)' item says 'Hover shows validation popover if disabled', and quiz ev9 question stem ('you hover over it to see a popover') + explanation ('hovering over the disabled Save button shows a bulleted list') describe HOVER behavior. The events panel actually uses <app-validation-button>, which is CLICK-to-reveal: a separate warning-triangle icon button to the left of the disabled Save button with (click)="toggle()" opening a CDK overlay (hover ValidationPopoverDirective is legacy/'do not use on new code' per CLAUDE.md). The substance of what's shown (a bulleted list of which fields need attention) is correct, and ev9's correct answer remains correct; only the hover-vs-click interaction framing is inaccurate. Flagged rather than fixed because rewording the question premise/explanation is a judgment call and the correct answer is unaffected.
- **`events-field-reference`** — QuickRef 'Notifications and Reminders' -> 'Cancellation' item states 'Attendees are notified of the cancellation.' The DeleteEvent handler (DeleteEvent.cs) only sets IsCancelled = true and SaveChanges — it creates NO notifications. So no attendee notification is actually sent on cancel in the backend. However, the UI confirm dialog text literally says 'Attendees will be notified.', so the training may be reflecting the UI's stated intent rather than implemented behavior. Ambiguous (UI-claim vs backend-reality), so flagged for human review instead of edited. Note quiz ev6's explanation only describes the dialog 'warns that attendees will be notified', which is an accurate description of the dialog text and need not change.

### vendors
- **`vendors-field-reference`** — The 'Row click' item and the Detail Dialog tab groups describe the detail dialog as having 'Info, Purchase Orders, and Scorecard tabs' (three), omitting the Catalog tab. The real vendor-detail-panel renders four tabs: Info, Purchase Orders, Catalog, Scorecard. The Catalog tab is intentionally documented in a separate module (vendors-catalog-*), so this may be a deliberate scoping choice rather than an error — left unfixed for human review (it is an omission, not a false statement).

---

## Fixes applied (already committed)

### Accounting (1)
- **`accounting-bank-statements-guide`** — The Article's match-status list described a Suggested line as 'Forge proposed a match. Review it, then confirm or reject.' There is no 'Reject' action in the real Bank Statements UI — a Suggested line offers only Confirm (check) and Ignore (visibility_off). The article's own 'Working the Queue' section, the walkthrough step, the quiz (ax6), and the Advanced Reference all correctly say Confirm/Ignore; only this bullet said 'reject'. Changed 'confirm or reject' to 'confirm or ignore it' in EN .cs and the matching Spanish bullet ('recházala' to 'ignórala') in es accounting.json. Markdown list spacing preserved; structure unchanged.

### Ai (4)
- **`ai-field-reference`** — QuickRef 'Data and API' group claimed the assistant list loads from GET /api/v1/ai/assistants and that the chat request body is {text, history[]}. The real ai.service.ts loads the list from GET /api/v1/ai-assistants (the separate AiAssistantsController, route 'api/v1/ai-assistants'), and the chat body record AssistantChatBody/AssistantChatCommand uses the field 'question' (sent as { question, history }), not 'text'. Fixed both to '/api/v1/ai-assistants' and '{question, history[]}' in .cs + es.json.
- **`ai-field-reference`** — QuickRef 'Clear Chat' item said the button is 'Disabled when there are no messages.' In ai.component.html the Clear Chat button lives in the chat header wrapped in @if (hasMessages()), so it is HIDDEN (not rendered) when there are no messages — never shown grayed-out/disabled. Corrected the description in .cs + es.json.
- **`ai-overview`** — Article body said the Clear Chat button 'is disabled when there are no messages.' Actual behavior: the button only renders (in the chat header) once hasMessages() is true, i.e. it is hidden, not disabled, when empty. Corrected the wording in .cs + es.json.
- **`ai-quiz`** — Quiz explanations for ai8 and ai12 stated the assistant list is loaded from GET /api/v1/ai/assistants. The real endpoint is GET /api/v1/ai-assistants (AiAssistantsController). Corrected both explanations in .cs + es.json (question/option structure and the single isCorrect-per-question invariant left unchanged).

### Announcements (4)
- **`announcements-admin-overview`** — Article described 'Team Leads Only' scope as reaching 'only the leads of the target teams' and implied Target Teams applies to it. Per CreateAnnouncement.BroadcastAnnouncement + GetActiveAnnouncements, TeamLeadsOnly targets ALL users with the Manager or Admin role company-wide (the code comment says 'team leads = Manager or Admin'); it ignores targetTeamIds entirely, the validator does not require Target Teams for it, and the UI showTeamSelector is true only for SelectedTeams/IndividualTeam. Corrected the scope description and the Target-Teams-appearance sentence to name the two team-based scopes explicitly. Mirrored in es.
- **`announcements-admin-field-reference`** — QuickRef 'Scope Options' entry for Team Leads Only said 'Only the leads of the target teams, not every member.' Same error as above — corrected to 'users in a leadership role (Manager or Admin), company-wide; does not use Target Teams.' Mirrored in es.
- **`announcements-admin-quiz`** — Quiz ann2 scenario ('go only to the leads of two specific teams') and its 'correct' answer ('Team Leads Only, then pick the two teams in Target Teams') describe behavior that does not exist: Team Leads Only has no team picker (showTeamSelector false) and broadcasts to ALL managers/admins company-wide, not to specific teams' leads. Reworded the scenario to 'company's team leads (managers), not every employee', changed correct option c to 'Team Leads Only', and corrected the explanation. Kept option ids a/b/c/d and exactly one correct (c). Mirrored in es.
- **`announcements-admin-quiz`** — Quiz ann3 explanation falsely claimed 'Team Leads Only also surfaces the team picker to choose whose leads are targeted.' The picker does not appear for TeamLeadsOnly. Corrected the trailing sentence (correct option b unchanged). Mirrored in es.

### Approvals (1)
- **`approvals-field-reference`** — The 'Workflows table' QuickRef item gives an explicit column enumeration ('Columns: workflow name, entity type, Active/Inactive status chip, and created date') that omits the Steps-count column. The real workflows DataTable (approval-workflow-editor.component.ts) defines columns name, entityType, isActive, stepsCount (header 'Steps', label confirmed in en.json approvals.cols.steps), createdAt, actions. Added 'steps count' to the enumeration in both EN .cs and ES .json; structure/group/item counts unchanged.

### Assets (1)
- **`assets-walkthrough`** — Step 2 'Search Assets' popover claimed search matches by 'name, manufacturer, model, or serial number'. The actual backend (AssetRepository.GetAssetsAsync) only searches Name, Manufacturer, and SerialNumber — Model is NOT a searched field. Removed 'model' from the searchable-fields list in both the EN .cs literal and the ES json (kept structure/side identical).

### CustomerReturns (2)
- **`customer-returns-using-the-ui`** — Status-chip color legend in the 'The Returns List' walkthrough step said 'purple = In Inspection'. The In Inspection chip uses class chip--primary, which maps to --primary (#0d9488 in light theme = teal, not purple). Other status colors in the legend (blue=Received/--info, amber=Rework Ordered/--warning, green=Resolved/--success, gray=Closed/--text-muted) are correct. Fixed 'purple' -> 'teal' in EN .cs and 'morado' -> 'verde azulado' in ES json.
- **`customer-returns-resolving-and-closing`** — Article said 'a green Resolve button appears in the panel'. The Resolve button in customer-return-detail-panel.component.html uses class action-btn action-btn--primary, which per the button taxonomy / _shared.scss is FILLED TEAL (var(--primary)); green is reserved for action-btn--create. Fixed 'green Resolve button' -> 'teal Resolve button' in EN .cs and 'botón verde Resolver' -> 'botón verde azulado Resolver' in ES json.

### Dashboard (2)
- **`dashboard-field-reference`** — Today's Tasks > Job List claimed jobs are 'sorted by due date (earliest first)'. The real widget (todays-tasks-widget.component.ts priorityTasks) sorts by statusColor priority — overdue, active, upcoming, completed — and the server (DashboardRepository) orders by stage SortOrder then BoardPosition; neither sorts by due date. The seeder's own Overview, Widgets Catalog, and quiz db10 already correctly say 'by status / overdue first'. Corrected the sort wording and the row-fields list (row shows title, job number, assignee avatar, status badge) in both EN .cs and ES .json.
- **`dashboard-quiz`** — Quiz db7's keyed-correct answer (option c) stated the Getting Started banner disappears 'when all onboarding steps are complete — profile, compliance forms, training' and the explanation said 'It cannot be manually dismissed while onboarding steps remain.' getting-started-banner.component.ts shows the steps are create-first-job / add-customer / set-up-track-types / receive-first-stock / explore-reports (capability-filtered), the banner hides when completedCount >= Math.min(3, steps.length) ('enough', not all), AND it has a dismiss() close button (so it CAN be manually dismissed). Rewrote option c to describe the real triggers (enough steps complete OR manual dismiss), changed distractor b to a clearly-false statement to preserve exactly one correct answer, and corrected the explanation. Applied to both EN .cs and ES .json (db7 still has exactly one isCorrect = c).

### Edi (2)
- **`edi-field-reference`** — The 'Save / Create (partner dialog)' item claimed 'Hovering when disabled shows a validation popover.' The real dialog wraps the submit button in <app-validation-button>, which is a click-to-reveal warning-triangle icon shown to the LEFT of the disabled button (toggled by click, not hover). The legacy hover ValidationPopoverDirective is not used here. Rewrote to describe the click-to-reveal warning icon. Fixed in both EN .cs and ES .json.
- **`edi-field-reference`** — The 'Submit button' item (Validation Rules group) claimed 'Hovering over the disabled button shows a validation popover listing which fields need attention.' Same root cause: the actual control is <app-validation-button> (click-to-reveal icon to the left of the button), not a hover popover on the button. Rewrote to describe the click-to-reveal warning icon. Fixed in both EN .cs and ES .json.

### Estimates (4)
- **`estimates-field-reference`** — QuickRef described the ConvertedToQuote status chip as 'purple' in two places (the 'Estimate Statuses' item and the 'Table Columns' Status item). The UI renders it via getStatusClass -> 'chip--primary', and .chip--primary uses --primary which is teal (#0d9488 light / #2dd4bf dark), not purple. Changed 'purple' -> 'teal' in EN (.cs) and 'morado' -> 'verde azulado' in ES (.json), both occurrences each.
- **`estimates-field-reference`** — QuickRef 'Save (dialog button)' claimed 'Hover shows validation popover listing violations'. The estimates tab wraps the Save button in <app-validation-button>, which the design system defines as a click-to-reveal warning-icon popover (NOT hover; the legacy hover ValidationPopoverDirective is not used here). Reworded EN to 'A warning icon next to it opens a click-to-reveal popover listing the violations' and ES correspondingly (removed 'Al pasar el cursor').
- **`estimates-overview`** — ES Article quoted the literal Part-column label for a part-less line as 'Monto global', but the actual in-app Spanish string (i18n key customers.estimates.lumpSum) is 'Suma global'. The EN training correctly quotes 'Lump sum' (matches en.json). Fixed the ES literal-label reference 'Una línea sin pieza muestra ...' from 'Monto global' to 'Suma global'. (EN needed no change.)
- **`estimates-field-reference`** — ES QuickRef 'Pieza del catálogo' item quoted the literal Part-column label as 'Monto global' ('la columna Pieza muestra entonces ...'), but the real ES UI string is 'Suma global' (i18n customers.estimates.lumpSum). Fixed that literal-label reference to 'Suma global'. (EN QuickRef already correctly says 'Lump sum'.)

### Expenses (5)
- **`expenses-overview`** — Article claimed the Search input filters by "description, category, or submitter name" and listed Status-filter options as only All/Pending/Approved/Rejected/SelfApproved. Backend ExpenseRepository.GetExpensesAsync searches Description+Category only (no user name), and the status dropdown also includes NeedsRevision. Corrected search to "description or category" and added NeedsRevision to the status-filter option list (EN .cs + ES .json).
- **`expenses-walkthrough`** — Step 1 said search works "by description, category, or submitter name" and "filter in real time as you type" — search is Description+Category only and applies on Enter (keyup.enter). Step 2 (Status Filter) omitted NeedsRevision from the dropdown options. Step 3 (Table) said "Click a row for details" but the main /expenses data-table has no rowClick/detail dialog. Fixed all three (EN .cs + ES .json).
- **`expenses-field-reference`** — QuickRef 'Search' item claimed filtering by "description, category, or submitter name" (code: Description+Category only), and 'Status Filter' item omitted NeedsRevision from the dropdown options. Corrected both items (EN .cs + ES .json).
- **`expenses-quiz`** — ex9 marked-correct option C was "Description, Category, and Submitted By name" with a matching explanation — backend search covers Description+Category only, so the correct answer was factually wrong (and no option was correct). Changed option C to "Description and Category" (still the single correct answer) and fixed the explanation. Also added NeedsRevision to ex7's explanation list of status-filter options. (EN .cs + ES .json).
- **`expenses-approval-queue`** — Walkthrough step 2 told users to narrow the queue by typing "a submitter, category, or description" — the approval queue uses the same Description+Category-only search (submitter name is not searchable). Removed the submitter claim (EN .cs + ES .json).

### Inventory (3)
- **`inventory-field-reference`** — QuickRef 'Location Form Fields (Create Location Dialog)' listed a 'Parent' dropdown field with a '-- None --' option. The actual Create Location dialog (inventory.component.html lines 532-555) has NO Parent control — only Name, Type, Barcode (conditional), Description are bound. The `parentId` FormControl is never rendered; parent is set programmatically by openCreateLocation(parentId): the page-header 'Add Location' button = top-level (no parent), the per-location 'Add child' button pre-sets that location as parent. Rewrote the Parent item to describe this accurately (kept the group at 5 items).
- **`inventory-field-reference`** — QuickRef 'Barcode' location-form item said it's a general optional text input on the location dialog. The barcode field only renders when Type is 'Bin' (@if locationType.value === 'Bin', inventory.component.html line 539). Added the 'only shown when Type is Bin' qualifier in EN and ES.
- **`inventory-field-reference`** — QuickRef 'Buttons and Actions' said the Create Location button has data-testid 'create-location-btn'. The actual button uses data-testid='add-location-btn' (inventory.component.html line 3); 'create-location-btn' does not exist anywhere in the inventory feature. Corrected to add-location-btn in EN and ES.

### Invoices (2)
- **`invoices-field-reference`** — Line Item 'Qty (required)' claimed 'Must be at least 1', but the create dialog uses Validators.min(0.0001) and the input has step="0.0001" min="0.0001" (fractional UoM-aware quantities, default 1). Corrected to 'defaults to 1; must be greater than 0 (fractional quantities allowed)' in EN .cs and ES .json.
- **`invoices-field-reference`** — Validation Rules 'Line Quantity' claimed 'Required. Must be >= 1', but the actual validator is Validators.min(0.0001). Corrected to 'Must be greater than 0 (fractional quantities allowed)' in EN .cs and ES .json.

### Kanban (4)
- **`kanban-overview`** — Priority left-border colors were wrong: training said 'green (Low), blue (Normal)'. Verified against PRIORITY_COLORS in forge-ui (priority-colors.const.ts, used by job-card.component.ts and the detail-panel dot): Low = #94a3b8 (gray/slate), Normal = #0d9488 (teal). High/Urgent (orange/red) were correct. Corrected Low→gray, Normal→teal in the overview body (EN .cs + ES json).
- **`kanban-walkthrough`** — Job Card step repeated the same wrong priority colors ('green=Low, blue=Normal'). Corrected to 'gray=Low, teal=Normal' to match PRIORITY_COLORS (Low #94a3b8, Normal #0d9488) in both EN and ES.
- **`kanban-field-reference`** — Three fixes. (1) 'Left Border Color' and 'Priority Levels' and 'Priority (sidebar)' items stated Low=green/Normal=blue; corrected to Low=gray/Normal=teal per PRIORITY_COLORS. (2) 'Explode BOM Button' claimed 'Each BOM line becomes a child job'; verified ExplodeJobBom.cs only creates child jobs for BOMSourceType.Make lines (Buy → purchasing suggestions, Stock → auto-reserved inventory), and the explodeBomConfirmMessage i18n key confirms 'all "Make" items'. (3) 'Lock Icon' item misquoted the column tooltip as 'Documents created at this stage cannot be voided.' — the actual irreversibleTooltip i18n value is 'Irreversible — jobs cannot move backward from this stage' (the 'cannot be voided' string is only the server MoveJobStage exception message, not the tooltip). All corrected in EN + ES.
- **`kanban-quiz`** — kb10 correct option and explanation claimed BOM explosion makes a child job for 'each BOM line'. Verified ExplodeJobBom.cs creates child jobs only for 'Make' BOM lines (Buy/Stock handled differently). Reworded correct option a and its explanation to 'each Make BOM line' (still clearly distinct from the wrong b/c/d options) in EN + ES; isCorrect flag and all option ids unchanged.

### Leads (3)
- **`leads-overview`** — Article stated the lost reason is 'optional but valuable'. In the real UI both lost-reason dialogs (leads.component.ts line 150 and lead-detail-panel.component.ts line 58) declare the reason FormControl with Validators.required and disable the Confirm button via [disabled]="lostFormGroup.invalid", plus the textarea has [required]="true". The reason is required, not optional. Changed to state it is required and you can't confirm without entering one (kept the 'also valuable' framing). Fixed in EN .cs and ES json.
- **`leads-quiz`** — Two quiz explanations contradicted the code: ld6 said the lost-reason dialog reason is '(optional but recommended)', and ld8 said 'The reason is optional — you can confirm without entering one.' The lost-reason control is Validators.required and the Confirm button stays disabled until a reason is entered, in both the pipeline drag-to-Lost dialog and the detail-panel dialog. Corrected both explanations to state the reason is required (correct-answer option ids and the single-isCorrect-per-question structure left unchanged). Fixed in EN .cs and ES json.
- **`leads-field-reference`** — The 'Confirm Lost (lost reason dialog)' QuickRef item said it 'Confirms marking the lead as Lost with the optional reason.' The reason is Validators.required and the button is disabled until filled. Changed to state the reason is required (Validators.required) and the button stays disabled until entered. Fixed in EN .cs and ES json; group/item counts unchanged.

### Mfa (1)
- **`mfa-overview`** — The Overview article claimed the login MFA challenge can be completed with ANY registered device ('You can still complete the challenge with any registered device (the same 6-digit TOTP flow applies to each)'). The code contradicts this: MfaService.CreateChallengeAsync selects only the default device (OrderByDescending(IsDefault).FirstOrDefault) and caches MfaChallengeData(userId, device.Id); ValidateChallengeAsync then validates the entered code ONLY against that single bound device's secret. There is no device-picker at the challenge screen (mfa-challenge.component) — the only alternative is a recovery code. Replaced the false clause with an accurate statement that the challenge is bound to the default device specifically and that a recovery code is the fallback if the default device is unreachable. Applied to both the EN .cs ContentJson and the ES mfa.json, preserving structure.

### Navigation (16)
- **`navigating-the-app-overview`** — Body called the app "QB Engineer". The real app name everywhere (About dialog, wordmark, ThemeService.appName) is "Forge". Changed to "Forge" in EN + ES.
- **`navigating-the-app-overview`** — Claimed the theme toggle lives in the user/avatar menu. In code the theme toggle is a separate sun/moon icon button in the header (app-header toggleTheme); the avatar menu contains Account Settings, About, language, Logout only. Corrected the header list item and the Theme Switching section (EN + ES).
- **`navigating-the-app-overview`** — Keyboard-shortcut claims were wrong: said '?' shows shortcuts, '/' focuses search, Ctrl+K opens a command palette. Actual (keyboard-shortcuts.service.ts + header): '/' opens the shortcuts help, Ctrl+K focuses global search, Escape closes; there is no '?' binding and no command palette. Rewrote the shortcut list (EN + ES).
- **`navigating-the-app-overview`** — Claimed the sidebar expands on hover and that state 'persists across sessions and devices'. There is no hover-to-expand (sidebar.scss has no :host hover width rule); expansion is via the chevron toggle at the top, and state is localStorage 'forge-sidebar-collapsed' (per-browser, not cross-device). Corrected (EN + ES).
- **`navigating-the-app-overview`** — Theme cross-tab sync was attributed to 'the storage event'; code uses BroadcastChannel ('forge-sync', broadcast.service.ts). Removed the incorrect mechanism phrase (EN + ES).
- **`navigating-the-app`** — Walkthrough step 1 (.sidebar) said icons 'expand to labeled menus when you hover'. No hover-expand exists; the sidebar is a click-to-drill group model with a chevron toggle. Rewrote the description (EN + ES). Step element/side unchanged.
- **`navigating-the-app`** — Walkthrough step 4 (.user-trigger) said the user menu lets you 'switch themes'. The theme toggle is a separate header icon, not in the avatar menu. Replaced with account/language/logout and noted the separate theme icon (EN + ES). Step element/side unchanged.
- **`navigation-field-reference`** — User-avatar item listed 'theme toggle (light/dark)' as a menu entry. Corrected to Account Settings / About / language / Logout, and clarified the theme toggle is a separate header icon (EN + ES).
- **`navigation-field-reference`** — Search-icon item said 'Press / to focus' the search; actual focus shortcut is Ctrl+K ('/' opens shortcuts help). Corrected (EN + ES).
- **`navigation-field-reference`** — Keyboard Shortcuts group was wrong: '/' = focus search, '?' = shortcuts help, Ctrl+K = command palette. Corrected to '/' = shortcuts help, Ctrl+K = focus search, Escape = close, and replaced the bogus command-palette item with the real Q-then-letter navigation chords (EN + ES). Item count kept at 4.
- **`navigation-field-reference`** — Theme System: 'Light theme' was labeled Default but ThemeService defaults to dark; localStorage key was 'themeMode' but is 'forge-theme'; sync was 'storage event' but is BroadcastChannel. Moved 'Default' to Dark theme, fixed the key, removed the wrong sync mechanism (EN + ES).
- **`navigation-field-reference`** — Section-grouping item listed a non-existent 'Finance' group. Replaced the divider/section example with the real top-level groups (Operations, Sales, Production, Inventory, Purchasing, People, Insights) and the Dashboard / main / pinned Shop Floor+Admin divider structure (EN + ES).
- **`navigation-quiz`** — nav1 correct option/explanation said 'press / to open global search'; actual is Ctrl+K. Changed option b text + explanation (EN + ES). Option ids and single isCorrect preserved.
- **`navigation-quiz`** — nav4 correct option said you switch theme via the avatar menu; it's a separate header sun/moon icon. Changed option b text + explanation to match (EN + ES). Structure preserved.
- **`navigation-quiz`** — nav6 marked '?' as the shortcut for the keyboard-shortcuts help; actual binding is '/'. '/' was not among the options, so changed the correct option's text from '?' to '/' and updated the explanation (EN + ES). Still exactly one isCorrect (option c).
- **`navigation-quiz`** — nav7 correct option/explanation said expand via chevron 'at the bottom' or by hovering. Toggle is at the top and there is no hover-expand. Corrected option a + explanation (EN + ES). Structure preserved.

### Notifications (2)
- **`notifications-reference`** — Mobile notifications route was stated as `/mobile/notifications`, but the mobile shell is mounted at `path: 'm'` in app.routes.ts (and the mobile-redirect guard navigates to `/m`), so the real route is `/m/notifications`. Fixed the path in both the English .cs Reference body and the Spanish JSON.
- **`notifications-walkthrough`** — Step 1 claimed the full Notifications page can be opened 'from the bell's View all link'. No such link exists: the bell button only toggles the slide-out panel, and the notification-panel template (tabs + mark-all-read + list + Dismiss-all footer) has no 'View all'/navigation link to /notifications. Removed the fabricated affordance, keeping the correct 'by navigating to Notifications' path, in both .cs and es.json.

### Onboarding (1)
- **`setting-up-your-profile`** — Security section claimed the kiosk PIN is a "4–6 digit PIN". The real app (account-security.component.ts) validates the PIN with pattern /^\d{4,8}$/ and its UI hint/placeholder say "4-8 digit" / "4-8 digits". Corrected to "4–8 digit PIN" in both the English .cs ContentJson and the Spanish onboarding.json (PIN de 4 a 8 dígitos).

### PageSpecific (1)
- **`page-specific-reference`** — Advanced Reference (Sales Tax & Currencies section) stated only the exchange-rate controller 'additionally requires the Admin role', implying /admin/currencies was open to any authenticated user. In code BOTH CurrenciesController and ExchangeRatesController carry [Authorize(Roles = "Admin")]. Corrected to 'both controllers additionally require the Admin role' in EN .cs and ES .json.

### Parts (5)
- **`parts-overview`** — The 'Creating a Part' section claimed the New Part fork dialog's 4 questions are 'procurement, inventory class, plus a question or two about traceability and inspection.' Verified against new-part-fork-dialog.component.ts/.html: the fork's 4 steps are Procurement Source, Inventory Class, optional Item Kind, and creation Mode (express vs guided). Traceability and inspection are NOT in the fork (they live in the workflow's Basics/Quality steps). Rewrote to name the real 4 questions in both EN .cs and ES .json.
- **`parts-walkthrough`** — Step 1 ('New Part' popover) said the fork asks 'a couple of questions about traceability and inspection.' Same refutation as parts-overview — the fork asks procurement, inventory class, optional item kind, and creation mode. Corrected the popover description in EN .cs and ES .json (element selector and side unchanged).
- **`parts-quiz`** — Quiz pt11 option b (the correct answer) described the fork as '(procurement, inventory class, traceability, inspection)'. Corrected to '(procurement, inventory class, optional item kind, and creation mode — express vs guided)' in EN .cs and ES .json. Quiz structure unchanged (18 questions, same ids/option-ids, exactly one isCorrect).
- **`parts-quiz`** — Quiz pt9 option b (correct answer) asserted 'Identity is always first, Activity → Files always last.' Verified against part-detail-layout-resolver.service.ts and part-detail-panel.component.html: the resolver returns [IDENTITY, ...middle, FILES] (Files always last) and Activity is rendered as a persistent footer below the tab body, explicitly NOT a tab. Changed to 'Files always last (Activity is a persistent footer below the tab body, not a tab)' in EN .cs and ES .json.
- **`parts-field-reference`** — The 'Detail Page Tab Layout' group's 'Resolver-driven' item stated 'Identity always first, Activity → Files always last.' Same refutation as pt9 — Activity is a persistent footer, not a tab. Corrected to 'Identity always first, Files always last. Activity is a persistent footer below the tab body, not a tab.' in EN .cs and ES .json. Group/item counts unchanged (23 groups).

### Payments (2)
- **`payments-field-reference`** — QuickRef 'Buttons and Actions' → 'Create Payment' item claimed the validation popover shows 'on hover when disabled'. The dialog uses <app-validation-button> (payment-dialog.component.html line 97), which is a click-to-reveal warning-icon button placed to the LEFT of the submit button — clicking the icon toggles the popover (validation-button.component.ts toggle()/(click)). It is NOT hover-triggered (hover is the deprecated legacy ValidationPopoverDirective per CLAUDE.md). Reworded EN+ES to describe the warning icon + count badge that is clicked.
- **`payments-field-reference`** — QuickRef 'Validation Rules' → 'Submit button popover' item claimed 'hovering over Create Payment shows a validation popover'. Same contradiction with the click-to-reveal <app-validation-button> stereotype. Reworded EN+ES to: a warning icon with an issue count appears next to Create Payment and is clicked to open the popover.

### Planning (1)
- **`planning-field-reference`** — QuickRef 'Mark Complete (check circle outline)' item said the button appears on 'each uncommitted entry'. That is contradictory/wrong: every entry on the cycle board is by definition committed, and the button (cycle-board.component.html: @if (!entry.completedAt)) actually renders on entries that are NOT yet completed. The Spanish file already had the correct wording ('en cada entrada no completada'). Fixed the English to 'Icon button on each not-yet-completed entry.' to match the code and the Spanish. No ES edit needed (already correct).

### PurchaseOrders (4)
- **`purchase-orders-walkthrough`** — Status chip color mislabeled: the 'Status Column' step said 'purple for Acknowledged'. The Acknowledged chip uses chip--primary, which resolves to var(--primary) = teal #0d9488 (per _shared.scss + _variables.scss), not purple. Changed 'purple' to 'teal' (ES 'morado' to 'turquesa').
- **`purchase-orders-walkthrough`** — Wrong table column selector: the 'Status Column' step targeted 'app-data-table thead th:nth-child(5)', but in the PO list table (poColumns order: PO#, Vendor, Job, Status, Lines, Ordered, Received, Expected, Created; table is not selectable/expandable so no leading th) the 4th header is Status — nth-child(5) is the Lines column. Changed selector to th:nth-child(4) in both EN .cs and ES .json (element strings kept identical).
- **`purchase-orders-field-reference`** — PO Statuses group: 'Acknowledged (purple chip)' is wrong — chip--primary is teal (var(--primary) #0d9488), not purple. Changed to 'Acknowledged (teal chip)' / ES 'Confirmada (chip turquesa)'.
- **`purchase-orders-field-reference`** — Blanket Release Fields, Release Status item: 'Sent (purple)' is wrong — getReleaseStatusClass maps Sent to chip--primary (teal), not purple. Changed to 'Sent (teal)' / ES 'Enviada (turquesa)'. (Open=info/blue, PartialReceived=warning/orange, Received=success/green, Cancelled=error/red all verified correct.)

### Quality (5)
- **`quality-reference`** — Advanced Reference listed the QC catalog capability as `CAP-QC-COMPLIANCE`, but the real code (CapabilityCatalog.cs:137) registers it as `CAP-QC-COMPLIANCE-FORMS`. There is no `CAP-QC-COMPLIANCE` code. Corrected the code name in both EN .cs and ES .json.
- **`quality-reference`** — Advanced Reference made an explicit named-enum claim: `RootCauseMethod` enum: 5 Whys / Fishbone / Fault Tree / 8D — but the real enum (forge.core/Enums/RootCauseMethod.cs) has SIX members: FiveWhy, Fishbone, FaultTree, EightD, Pareto, IsIsNot. Added the two missing members (Pareto, Is-Is Not) in EN and ES.
- **`quality-overview`** — Overview enumerated root-cause methods as '(5 Whys, Fishbone, Fault Tree, 8D)' as the complete set, but the RootCauseMethod enum has six members. Added Pareto and Is/Is-Not for consistency with the verified enum, in EN and ES.
- **`quality-field-reference`** — Field Reference 'Phase-only fields' item listed the root-cause method values as '(5 Whys / Fishbone / Fault Tree / 8D)', omitting the enum's Pareto and IsIsNot members. Added them in EN and ES.
- **`quality-quiz`** — Quiz qc7 explanation enumerated the root-cause method as '(5 Whys, Fishbone, Fault Tree, 8D)' as if complete; the enum has six members. Added Pareto and Is/Is-Not to the explanation only (the qc7 option text uses 'e.g.,' and was left untouched). EN and ES.

### Quotes (1)
- **`quotes-field-reference`** — The 'Line Item Fields (Create Dialog — Main Area)' group claimed Qty 'Minimum 1', and the 'Validation Rules' group claimed 'Line — Quantity ... Must be >= 1'. The actual create dialog (quote-dialog.component.ts) uses Validators.min(0.0001) with the input bound to min="0.0001" step="0.0001", and the Overview Article + Advanced Reference modules already correctly state fractional quantities to four decimals (min 0.0001) are allowed. Corrected both items to 'Minimum 0.0001 — fractional quantities to four decimals are allowed (step 0.0001)' and '>= 0.0001', in both the English .cs ContentJson and the Spanish quotes.json. Structure unchanged (same group/item counts).

### Reports (5)
- **`reports-overview`** — Stated the library has 27 pre-built report templates in 3 places (intro, sidebar description, '### Pre-Built Report Templates (27)'), but the real UI sidebar (reports.component.ts) renders 28 reports — and the Overview body itself lists all 28 by name. Corrected 27 -> 28 (EN .cs + ES .json).
- **`reports-walkthrough`** — Sidebar step popover said 'lists all 27 pre-built reports'; actual sidebar has 28 report-nav items. Corrected 27 -> 28 (EN .cs + ES .json).
- **`reports-field-reference`** — Buttons-and-actions item 'Report sidebar items' said '27 reports available'; actual is 28. Corrected 27 -> 28 (EN .cs + ES .json).
- **`reports-field-reference`** — Per-entity field counts in the Entity Sources group were wrong for 9 of 28 entities vs GetReportEntities.cs (the Overview defines the count as total fields including related-entity fields): Jobs 33->35, Parts 19->17, Time Entries 14->15, Invoices 19->20, Sales Orders 17->20, Quotes 15->16, Downtime Logs 13->14, Customer Returns 17->19, Sales Order Lines 14->15. Parts was impossible under any interpretation (claimed 19 > actual total 17). Fixed all 9 to the actual total counts (EN .cs + ES .json).
- **`reports-reference`** — Per-Report Cheat Sheet header said 'the 27 pre-built reports'; actual is 28 (the cheat-sheet body enumerates all 28). Corrected 27 -> 28 (EN .cs + ES .json).

### SalesOrders (3)
- **`sales-orders-field-reference`** — Credit Terms item listed "2/10 Net 30" as an available payment term. The CreditTerms enum and CREDIT_TERMS_OPTIONS (used by the SO dialog) only contain Due on Receipt, Net 15, Net 30, Net 45, Net 60, Net 90 — there is no 2/10 Net 30. Removed the fabricated option (EN + ES).
- **`sales-orders-field-reference`** — Qty field said "Must be at least 1" and the Line — Quantity validation rule said "Must be >= 1". Actual validator is Validators.min(0.0001) with step=0.0001 min=0.0001 (fractional quantities allowed), as the Advanced Reference module itself correctly documents. Corrected both occurrences to >= 0.0001 / fractional allowed (EN + ES).
- **`sales-orders-reference`** — Schedule Tab section called it a "three-milestone timeline". ScheduleTimelineComponent.buildMilestoneItems() builds 7 milestones (poOrderBy, materialsNeededBy, productionStartBy, productionCompleteBy, qcCompleteBy, shipBy, deliveryDate), each rendered as a milestone card. Removed the incorrect "three" count, keeping the order→production→ship phase summary (EN + ES).

### Search (3)
- **`search-overview`** — Article claimed an Escape keyboard shortcut ('Escape — Close the search results overlay and clear the search input'). The real app-header.component.ts has NO Escape handling anywhere — the only keyboard handler is the document:keydown for Ctrl+K (focuses .search-input). The overlay is closed by blur (onSearchBlur hides after 200ms) or by clicking a result. Replaced the Escape bullet with an accurate 'Click away (blur)' bullet and noted Ctrl+K is the only shortcut; also corrected the click-result bullet to mention the input is cleared. Mirrored in es.json.
- **`search-walkthrough`** — Step 3 popover description told users 'Press Escape to close results and clear the input.' No Escape handler exists in the component. Changed to: Ctrl+K is the only keyboard shortcut; to dismiss the overlay, click away from the box. Mirrored in es.json (walkthrough structure unchanged: same 3 steps, elements, and popover.side).
- **`search-field-reference`** — QuickRef had two false Escape claims: (1) Keyboard Shortcuts group 'Escape' item ('Close the search results overlay and clear the input. Also unfocuses the search input.') and (2) Search Input group 'Clear behavior' item ('Navigating to a result or pressing Escape clears...'). No Escape handler exists. Replaced the 'Escape' shortcut item with a 'Click away (blur)' item (keeping the group at exactly 3 items) describing the real 200ms-delay blur close that does NOT clear the typed text, and corrected 'Clear behavior' to drop Escape. Mirrored in es.json; all group/item counts unchanged.

### TimeTracking (4)
- **`time-tracking-field-reference`** — The 'EntryType' backend field was described as 'Enum: Run (default), Setup' — only two values. The real TimeEntryType enum (forge.core/Enums/TimeEntryType.cs) has seven: Setup, Run (default), Teardown, Inspection, Rework, Wait, Other. Corrected the value list and description in both EN .cs and ES .json.
- **`time-tracking-reference`** — Reference body 'Time -> Job Costing' section described EntryType as '(Run default, or Setup) separates direct production time from setup time' — same two-value error. Corrected to the full seven-value enum in both EN .cs and ES .json.
- **`time-tracking-field-reference`** — The 'Log Entry (dialog save)' button was described as 'Shows validation popover on hover when invalid.' The template uses <app-validation-button>, which is click-to-reveal (its trigger is (click)="toggle()"); hover is the legacy ValidationPopoverDirective, not used here. Per CLAUDE.md the stereotype is explicitly click-to-reveal. Corrected to a click-to-reveal validation icon in both files.
- **`time-tracking-field-reference`** — The first 'Icon' column was described as showing a 'spinning timer icon for active timers'. The template applies class 'type-icon--running' to a static 'timer' icon and the SCSS only sets its color to green (var(--success)) — there is no spin animation. Corrected to 'green-highlighted timer icon' / 'muted timer icon' in both files.

### admin (1)
- **`admin-field-reference`** — Avatar Color field claimed a '12-color button grid', but the user form's avatarColors array in forge-ui/src/app/features/admin/admin.component.ts (lines 309-312) contains exactly 10 hex colors. Corrected '12-color' to '10-color' in both the EN .cs ContentJson and the ES admin.json.

### backlog (4)
- **`backlog-field-reference`** — Save button item claimed 'Hover shows a validation popover listing violations'. The job dialog actually wraps the Save button in the <app-validation-button> stereotype (ValidationButtonComponent), not ValidationPopoverDirective: validation is surfaced by a click-to-reveal red warning-triangle icon with an issue count to the LEFT of the Save button, not by hovering the disabled button. Reworded the value to describe the click-to-reveal warning icon. (EN .cs + ES .json)
- **`backlog-quiz`** — Question bl13 was premised on hovering ('What happens when you hover over a disabled Save button...') and its correct option + explanation asserted the dialog 'uses the ValidationPopoverDirective ([appValidationPopover])' with a hover-triggered popover. Verified job-dialog.component.html/.ts use <app-validation-button> (ValidationButtonComponent): a clickable warning-triangle icon (matTooltip 'Show validation errors', (click)=toggle()) opens the popover — click-to-reveal, not hover, and not the directive. Reworded the question stem, the correct option (c), and the explanation to match; kept the same question id, option ids (a/b/c/d), and exactly one isCorrect (c). (EN .cs + ES .json)
- **`backlog-field-reference`** — Card View Elements > 'Priority dot + name' claimed the dot background comes 'from PRIORITY_COLORS'. The card grid (BacklogCardGridComponent.priorityDotColor) uses its OWN hardcoded map keyed Critical/High/Medium/Low (#dc2626/#f97316/#d97706/#22c55e, gray fallback #94a3b8) — NOT PRIORITY_COLORS. Because the keys don't match the real priority names (Low/Normal/High/Urgent), only Low and High resolve and Normal/Urgent fall back to gray. The seeder's own Advanced Reference documents this exact gap. Reworded to point to the card grid's priorityDotColor map; left the Table Columns > Priority item (which correctly uses PRIORITY_COLORS) unchanged. (EN .cs + ES .json)
- **`backlog-quiz`** — Question bl12 explanation overclaimed that the PRIORITY_COLORS constant is 'used for both the table dot and the card view dot'. The card view resolves dot colors through a separate, non-matching map (verified in code and in this seeder's Advanced Reference). Kept the question/answer (Amber #f59e0b is the canonical/table color) and only corrected the explanation's final sentence to scope PRIORITY_COLORS to the table dot and note the card-view divergence. (EN .cs + ES .json)

### calendar (1)
- **`calendar-walkthrough`** — Step 5 element selector referenced a non-existent CSS class for the Day-view container: ".calendar-grid,.week-grid,.day-list". The real Day-view container class is ".day-view" (no ".day-list" exists anywhere in the calendar feature — calendar.component.html line 161 + .scss confirm ".day-view"/".day-view__list"). In Day view the step would highlight nothing. Corrected the third alternative to ".day-view" in both the English .cs ContentJson and the Spanish .json (element selectors are not translated, so both files held the identical value). Structure preserved: same step count (5), same popover title/side ("top").

### events (3)
- **`events-overview`** — Reminder timing was wrong. Content said the reminder is sent '15 minutes before an event starts.' Per EventReminderJob.cs the reminder is sent 30 minutes before the event (reminderWindow = now.AddMinutes(30); job comment 'sends reminder notifications 30 minutes before events start'); the 15-minute figure is actually the Hangfire RUN interval ('*/15 * * * *' in Program.cs). Corrected the Automatic Reminders paragraph to 'runs a background job every 15 minutes that sends reminder notifications 30 minutes before an event starts' in both EN .cs and ES .json.
- **`events-field-reference`** — QuickRef 'Notifications and Reminders' group: the '15-Minute Reminder' item conflated the job run interval with the lead time. Relabeled to 'Event Reminder' and corrected value to 'A Hangfire background job runs every 15 minutes and sends reminder notifications 30 minutes before the event starts' (EN .cs + ES .json), matching EventReminderJob.cs / Program.cs.
- **`events-quiz`** — Quiz ev5 correct answer (option c) and explanation stated the notification is sent '15 minutes before the event starts.' Code sends it 30 minutes before, with the job running every 15 minutes. Corrected option c to 'A Hangfire background job (running every 15 minutes) sends a notification 30 minutes before the event starts' and updated the explanation accordingly (EN .cs + ES .json). Option c remains the single correct answer; option ids and the other distractors unchanged.

### vendors (6)
- **`vendors-field-reference`** — Scorecard 'Overall Grade' chip colors were wrong. VendorScorecardTabComponent.gradeClass() maps A=success, B=info, C=warning, D=error, F=error — but the QuickRef said 'B = success' and 'D = warning'. Corrected to 'B = info (blue)' and 'D = error' in both en .cs and es .json.
- **`vendors-field-reference`** — Payment Terms dropdown list was wrong. The flat vendor dialog binds PAYMENT_TERMS_OPTIONS (credit-terms.const.ts) = Due on Receipt, Net 15, Net 30, Net 45, Net 60, COD (plus a blank option). The module listed 'Net 10, Net 15, Net 30, Net 45, Net 60, Net 90, 2/10 Net 30' — Net 10, Net 90 and 2/10 Net 30 do not exist in the vendor options and COD was missing. Corrected the dropdown list.
- **`vendors-field-reference`** — Purchase Orders tab 'Status' chip values were wrong. vendor-detail-panel getPoStatusClass/getPoStatusLabel render Draft, Submitted, Acknowledged, Partially Received, Received, Closed, Cancelled (PurchaseOrderStatus enum). The module said '(Draft, Sent, Partially Received, Received, Cancelled)' — 'Sent' is not a real status (it's Submitted) and Acknowledged/Closed were omitted. Corrected the status list.
- **`vendors-quiz`** — Quiz vn7 correct answer (payment terms) listed 'Due on Receipt, Net 10, Net 15, Net 30, Net 45, Net 60, Net 90, and 2/10 Net 30' which does not match the actual PAYMENT_TERMS_OPTIONS. Corrected option b text and the explanation to 'Due on Receipt, Net 15, Net 30, Net 45, Net 60, and COD'. Exactly one isCorrect preserved.
- **`vendors-walkthrough`** — Walkthrough step 2 (Search Vendors) said 'Type your search term and press Enter to filter the list.' VendorsComponent wires searchControl.valueChanges through a 300ms debounce that auto-fires loadVendors(); pressing Enter is optional (keyup.enter), not required. Reworded to describe the debounced auto-filter with Enter as an immediate-apply shortcut, in both en and es.
- **`vendors-quiz`** — Quiz vn8 was built on a false premise ('you type but nothing happens — you need to press Enter') that contradicts the debounced auto-search; none of its 4 options was actually correct. Rewrote the question to ask how the search applies (correct answer: auto-filters on a debounce, Enter applies immediately), keeping the same id, 4 option ids (a/b/c/d) and exactly one isCorrect, in both en and es.

### Seeders reviewed with no changes needed

`Automations`, `Chat`, `Customers`, `Employees`, `LeadScoring`, `Maintenance`, `Mrp`, `Payables`, `Payroll`, `ProductionLots`, `Purchasing`, `Scheduling`, `Shipments`, `ShopFloor`, `compliance`
