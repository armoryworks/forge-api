namespace Forge.Core.Enums;

/// <summary>Lifecycle of a <see cref="Entities.SequenceInstance"/> (a run of a published definition).</summary>
public enum SequenceInstanceStatus
{
    Running,
    Completed,
    Cancelled,
}
